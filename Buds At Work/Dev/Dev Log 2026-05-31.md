# Dev Log 2026-05-31

## [23:13] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Good progress. From the screenshot I can see your Facebook Page ID is `771212859402042`. The code and cron are already built — you just need the three env vars.

**Next steps from exactly where you are:**

**1. Get your Instagram ID** — in the Explorer query box, replace `me/accounts` with:
```
771212859402042?fields=instagram_business_account
```
Hit Submit. Copy the `id` from `instagram_business_account` — that's your `META_IG_USER_ID`.

**2. Make the token permanent** — you need your App ID a

---
## [23:13] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
The shared link isn't publicly accessible — Claude share links require authentication to view, so I can't fetch the content.

Can you paste the relevant parts here directly? Or describe what was in that conversation and I'll pick up from there.

---
## [23:14] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Got it — I can see the full context now. You're at the point where you have a short-lived token and need to finish steps 2 and 3. From the earlier screenshot I can see your Facebook Page ID is already `771212859402042`.

Here's exactly where you are and what's left:

**Do this now in the Explorer:**

1. Run this to get your Instagram ID:
   ```
   771212859402042?fields=instagram_business_account
   ```
   Copy the `id` from `instagram_business_account` → that's `META_IG_USER_ID`

2. Go to your 

---
## [23:15] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
You're in the wrong place — this is the Instagram Platform use case setup page (webhooks, add account), not the Graph API Explorer.

**Ignore this page entirely.** Open a new tab and go directly to:

👉 **developers.facebook.com/tools/explorer**

That's a completely separate tool. Once you're there, screenshot it and I'll walk you through the next step.

---
## [23:22] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Go to the Graph API Explorer: **developers.facebook.com/tools/explorer**

Once you're there:

**Step 1 — Get your Instagram ID**

In the query box at the top, type:
```
771212859402042?fields=instagram_business_account
```
Hit **Submit**. You'll see a JSON result — copy the number next to `"id"` inside `instagram_business_account`. Save it.

---

**Step 2 — Get your App ID and Secret**

Open a new tab → **developers.facebook.com/apps** → click your app → **Settings → Basic**. Copy the **App ID**

---
## [23:23] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
You need three values:

1. **APP_ID** — the number from your app's Settings → Basic page (looks like `26367220986288712`)

2. **APP_SECRET** — the string next to "App Secret" on that same page (click **Show** to reveal it, then copy it)

3. **fb_exchange_token** — the token currently sitting in the **Access Token box** in the Graph API Explorer (the long string starting with `EAF...`)

So the final URL looks like:
```
https://graph.facebook.com/v21.0/oauth/access_token?grant_type=fb_exchange_tok

---
## [23:24] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Replace the placeholder words at the end of the URL with your real values:

```
https://graph.facebook.com/v21.0/oauth/access_token?grant_type=fb_exchange_token&client_id=REPLACE_THIS&client_secret=REPLACE_THIS&fb_exchange_token=REPLACE_THIS
```

So concretely:

- `client_id=` → paste your App ID right after the `=`
- `client_secret=` → paste your App Secret right after the `=`  
- `fb_exchange_token=` → paste the long `EAF...` token right after the `=`

Example of what it looks like filled in:


---
## [23:25] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
It's already in the Graph API Explorer — it's the token you generated earlier when you clicked **Generate Access Token**.

Go to **developers.facebook.com/tools/explorer** and look at the **Access Token** box near the top right. The long string starting with `EAF...` that's sitting there — that's it.

Copy the whole thing and paste it after `fb_exchange_token=` in the URL.

---
## [23:27] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Got it. Now do this:

1. Copy that `access_token` value (the `EAF2s2...` string)

2. Go back to the **Graph API Explorer**

3. **Replace** the token in the Access Token box with this new one (paste it in, replacing whatever was there)

4. In the query box, run:
```
me/accounts
```

5. In the result, find **Buds At Work** and copy the `access_token` shown next to it — **that's your permanent `META_ACCESS_TOKEN`**

Screenshot the result and I'll confirm which value to grab.

> **ADR signal detected.** Consider running `npx tsx scripts/vault-adr.ts` to record this decision formally.

---
## [23:29] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Perfect. You have everything you need from that result:

- **`META_ACCESS_TOKEN`** → `EAF2s2QiaGkgBRpvgg5qnfmNKh1xbnUXZC5Nx...` (the full `access_token` next to Buds At Work)
- **`META_FB_PAGE_ID`** → `771212859402042`

Now get your Instagram ID — run this in the Explorer query box:
```
771212859402042?fields=instagram_business_account
```

Hit Submit and paste the result here.

---
## [23:30] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
You have all three values. Now add them to Vercel:

1. Go to **vercel.com** → your `budsatworkaus` project → **Settings → Environment Variables**
2. Add these three (set Environment to **Production**):

| Name | Value |
|---|---|
| `META_ACCESS_TOKEN` | `EAF2s2QiaGkgBRpvgg5qnfmNKh1xbnUXZC5Nx...` (the full token) |
| `META_FB_PAGE_ID` | `771212859402042` |
| `META_IG_USER_ID` | `17841476820244932` |

3. After saving all three, go to **Deployments** → latest deployment → **⋯ menu → Redeploy**

Onc

---
## [23:35] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
I can't call your production endpoint directly from here — I'd need your `CRON_SECRET` env var value to authenticate it.

The easiest way to test it yourself:

**Option 1 — Vercel dashboard**
Go to **vercel.com** → your project → **Settings → Cron Jobs** → find `marketing-metrics` → click **Run Now**

**Option 2 — Terminal**
```bash
curl https://budsatworkaus.vercel.app/api/cron/marketing-metrics \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```
(Replace `YOUR_CRON_SECRET` with the value from 

---
## [23:37] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
The cron is already in vercel.json at line 3-6 — `30 19 * * *` (UTC), which is 5:30am Brisbane. It's there but just not showing in the Vercel dashboard yet because it needs a deployment to register.

It's not showing because you haven't redeployed after adding the env vars yet. Once you redeploy, it'll appear in the dashboard and you can hit **Run Now** to test it immediately.

Go to **Vercel → Deployments → latest → ⋯ → Redeploy** and it'll all come online together.

---
## [23:39] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Pushed. Vercel will now build and deploy automatically — takes about 2-3 minutes. Once it's done the cron will be registered and the env vars will be active.

To test immediately after the deploy finishes, go to **Vercel → your project → Settings → Cron Jobs** and you'll see `marketing-metrics` listed. Hit **Run Now** and it'll pull the first batch of Meta data into the scoreboard.

---
## [23:43] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
It worked. `channels_written: 2` means both Instagram and Facebook data was pulled and written to the scoreboard. No errors.

The Marketing Studio scoreboard will now auto-fill every morning at 5:30am Brisbane without you touching anything.

---
