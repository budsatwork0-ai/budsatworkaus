# Dev Log 2026-05-29

## [12:02] Session 24850c62

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/075_bud_improvements.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Error: Failed to run sql query: ERROR: 42710: policy "admin_service_bud_improvements" for table "bu

**Dev OS agents:** bud-researcher

**Files changed:**
- `/tmp/bud-pr46-fix/src/agents/shared/schemas.ts` *(re-edited)*
- `src/lib/bud/health.ts`
- `supabase/migrations/072_dev_os_sessions.sql`
- `supabase/migrations/074_bud_evidence.sql` *(re-edited)*
- `supabase/migrations/075_bud_improvements.sql`

**Summary:**
Done. Two fixes landed:

**`src/lib/bud/health.ts`** (main branch) — `computeDeploymentState` now only marks the surface as `failed` when the latest event is a non-Preview environment failure. PR preview build errors downgrade to `stale` instead of blocking the pipeline. The preflight check will clear as soon as the next production deployment succeeds.

**PR branch fix** — pushed `fix/pr46-zod-record` to origin with the corrected `z.record(z.string(), z.unknown())` calls. You'll need to merge th

> **Re-edit detected** on `/tmp/bud-pr46-fix/src/agents/shared/schemas.ts`, `supabase/migrations/074_bud_evidence.sql` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:05] Session 24850c62

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/075_bud_improvements.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Error: Failed to run sql query: ERROR: 42710: policy "admin_service_bud_improvements" for table "bu

**Dev OS agents:** bud-researcher

**Files changed:**
- `/tmp/bud-pr46-fix/src/agents/shared/schemas.ts` *(re-edited)*
- `src/lib/bud/health.ts`
- `supabase/migrations/072_dev_os_sessions.sql`
- `supabase/migrations/074_bud_evidence.sql` *(re-edited)*
- `supabase/migrations/075_bud_improvements.sql`

**Summary:**
Pushed. Vercel will pick up the push to `main` and start a production deployment. 

The commit includes:
- **Preflight fix** — Preview environment failures no longer block the pipeline
- **29 migration policy guards** — all `CREATE POLICY` statements across migrations 042–066 are now idempotent

Once the production deployment completes and the webhook fires a `success` event, the preflight check will clear automatically.

> **Re-edit detected** on `/tmp/bud-pr46-fix/src/agents/shared/schemas.ts`, `supabase/migrations/074_bud_evidence.sql` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:23] Session 3fec8dc6

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/072_dev_os_sessions.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> https://budsatwork.com/dashboard/agents, any time I click onto the agents page, i get this error: Ap

**Files changed:**
- `src/app/(app)/dashboard/agents/page.tsx`
- `src/app/(app)/dashboard/mission-control/_components/OverviewV2.tsx` *(re-edited)*

**Summary:**
Clean. Two changes made:

1. **[OverviewV2.tsx](src/app/(app)/dashboard/mission-control/_components/OverviewV2.tsx)** — Added `suppressHydrationWarning` to all three elements that render `relativeTime()` output (lines 1015, 1019, 1039). In React 19 / Next.js 15, `Date.now()` differences between SSR and client hydration can escalate to a thrown error that triggers the error boundary — this suppresses that mismatch.

2. **[agents/page.tsx](src/app/(app)/dashboard/agents/page.tsx)** — Fixed the red

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewV2.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:26] Session 3fec8dc6

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/072_dev_os_sessions.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> https://budsatwork.com/dashboard/agents, any time I click onto the agents page, i get this error: Ap

**Files changed:**
- `src/app/(app)/dashboard/agents/page.tsx`
- `src/app/(app)/dashboard/mission-control/_components/OverviewV2.tsx` *(re-edited)*

**Summary:**
Pushed. Vercel's git integration will pick up commit `674b995` and deploy to production now. Once it's live, clicking the Agents nav item will redirect cleanly to `/dashboard/mission-control` (overview tab) with no hydration error.

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewV2.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [16:19] Session 3fec8dc6

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/072_dev_os_sessions.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> https://budsatwork.com/dashboard/agents, any time I click onto the agents page, i get this error: Ap

**Files changed:**
- `src/app/(app)/dashboard/agents/page.tsx`
- `src/app/(app)/dashboard/mission-control/_components/DevOsTab.tsx` *(re-edited)*
- `src/app/(app)/dashboard/mission-control/_components/OverviewV2.tsx` *(re-edited)*

**Summary:**
Pushed. Vercel will deploy shortly.

If the "Application error" still appears after this deployment, the issue isn't a `relativeTime` hydration mismatch — at that point I'd recommend adding a `mission-control/error.tsx` boundary to capture the actual JS error message from the browser, since all known time-sensitive renders now have `suppressHydrationWarning`.

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/DevOsTab.tsx`, `src/app/(app)/dashboard/mission-control/_components/OverviewV2.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
