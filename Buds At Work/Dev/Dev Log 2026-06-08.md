# Dev Log 2026-06-08

## [08:50] Session 0aeb6d5e

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Files changed:**
- `src/lib/bud/improvement-executor.ts` *(re-edited)*

**Summary:**
Clean. Here's the full diagnosis and what was fixed:

---

## Audit findings

**Root cause: one recurring failure pattern across all agent PRs**

Every failing deployment dies at the same point: TypeScript strict-mode type-check passes compilation but fails when checking types (the `Checking validity of types...` step, ~35–40s into the build).

**Two symptoms, one cause:**

| Error | File | Problem |
|---|---|---|
| `not assignable to parameter of type 'never'` | `src/agents/quote-triage/output-

> **Re-edit detected** on `src/lib/bud/improvement-executor.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [08:56] Session dbe03807

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Dev OS agents:** bud-factory

**Files changed:**
- `src/app/(app)/dashboard/growth-hq/page.tsx` *(re-edited)*
- `src/app/(app)/dashboard/story-engine/journal/_components/QuickCaptureWidget.tsx` *(re-edited)*
- `src/app/(app)/dashboard/story-engine/opportunities/page.tsx`
- `src/app/api/growth-hq/route.ts` *(re-edited)*
- `src/app/api/journal/quick-capture/route.ts` *(re-edited)*
- `src/app/api/story-drafts/generate/route.ts`
- `src/app/api/story-drafts/suggest/route.ts`
- `src/app/api/story-opportunities/route.ts`
- `src/lib/story/draft-generator.ts`
- `src/types/story-engine.ts`

**Summary:**
tsc, lint, build all clean. Here's what was built:

---

**New files**

- [src/lib/story/draft-generator.ts](src/lib/story/draft-generator.ts) — shared `generateDraftForOpportunity()` helper. Single source of generation logic: loads opportunity context, enforces consent gates, builds the Story Bible prompt, calls Haiku, persists to `story_drafts`. Both routes use it.
- [src/app/api/story-drafts/suggest/route.ts](src/app/api/story-drafts/suggest/route.ts) — `POST /api/story-drafts/suggest`. Takes

> **Re-edit detected** on `src/app/(app)/dashboard/growth-hq/page.tsx`, `src/app/(app)/dashboard/story-engine/journal/_components/QuickCaptureWidget.tsx`, `src/app/api/growth-hq/route.ts`, `src/app/api/journal/quick-capture/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:10] Session 0aeb6d5e

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Files changed:**
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/schema-guard.ts`

**Summary:**
Here's what was built and why each decision was made:

---

## What was built

### [schema-guard.ts](src/lib/bud/schema-guard.ts) — new module (248 lines)

**`extractSchema(src)`** — depth-tracking block reader, not a regex over the full file. It finds `Tables: {`, extracts the block character-by-character tracking brace depth and string literals, then for each table entry jumps the regex cursor past the table's closing `}` before continuing — so `Row:`, `Insert:`, `Update:` inside a table's blo

> **Re-edit detected** on `src/lib/bud/improvement-executor.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:21] Session 0aeb6d5e

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Files changed:**
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/schema-guard.ts`

**Summary:**
Now I have the full picture. Let me write the complete audit.

---

## Agent audit — current state

### Deployment health (unchanged from first audit)

The list is frozen — no new agent PRs since our session started. Our local fixes haven't been committed, so the live executor is still running unfixed code.

```
Total visible:  20 deployments
ERROR:          12  (60%)
READY:           8  (40%)
Newest ERROR:   bud/improve-agent-quote-triage-... (3h ago, same as before)
```

---

### Three structu

> **Re-edit detected** on `src/lib/bud/improvement-executor.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:26] Session 0aeb6d5e

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Files changed:**
- `src/app/api/webhooks/vercel/route.ts`
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/schema-guard.ts`

**Summary:**
Pushed. Vercel will pick it up now.

Three things shipped:

- **Schema guard** — `src/lib/bud/schema-guard.ts` parses `Database.public.Tables` and blocks patches before they're written if they reference tables or columns that would resolve to `never` at build time
- **Banned-path + TOOLCHAIN_NOTES** — patches targeting `src/agents/` are rejected immediately; the LLM now sees the exact list of 20 valid tables in every prompt
- **Feedback loop fix** — vercel webhook dedup extended to 24h and now i

> **Re-edit detected** on `src/lib/bud/improvement-executor.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:45] Session 33a9b821

**Task:** <ide_selection>The user selected the lines 1 to 17 from /Users/jacksontaylor/budsatwork/supabase/migrations/107_silvan_consent_granted.sql: -- 107_silvan_consent_granted.sql -- Records Silvan's granted consent status. -- Consent obtained and confirmed by Jackson Taylor on 2026-06-06. -- -- Effect: A

**Files changed:**
- `src/app/api/cron/vercel-repair/route.ts` *(re-edited)*
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/repair-quarantine.ts` *(re-edited)*
- `supabase/migrations/109_repair_quarantine.sql`

**Summary:**
All done. Here's what was built:

---

## Files changed

**New files:**
- [supabase/migrations/109_repair_quarantine.sql](supabase/migrations/109_repair_quarantine.sql) — `bud_repair_quarantine` table (branch, commit SHA, deployment ID, failing file/line, error text, source agent, attempt count, status, blocked_until). Unique on `branch`. Service-role-only RLS.
- [src/lib/bud/repair-quarantine.ts](src/lib/bud/repair-quarantine.ts) — quarantine helpers: `checkQuarantine`, `recordFailedAttempt`, `

> **Re-edit detected** on `src/app/api/cron/vercel-repair/route.ts`, `src/lib/bud/improvement-executor.ts`, `src/lib/bud/repair-quarantine.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
