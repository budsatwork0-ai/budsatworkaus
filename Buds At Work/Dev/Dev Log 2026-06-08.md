# Dev Log 2026-06-08

## [08:50] Session 0aeb6d5e

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Files changed:**
- `src/lib/bud/improvement-executor.ts` *(re-edited)*

**Summary:**
Clean. Here's the full diagnosis and what was fixed:

---

## Audit findings

**Root cause: one recurring failure pattern across all agent PRs**

Every failing deployment dies at the same point: TypeScript strict-mode type-check passes compilation but fails when checking types (the `Checking validity of types...` step, ~35–40s into the build).

**Two symptoms, one cause:**

| Error | File | Problem |
|---|---|---|
| `not assignable to parameter of type 'never'` | `src/agents/quote-triage/output-

> **Re-edit detected** on `src/lib/bud/improvement-executor.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [08:56] Session dbe03807

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Dev OS agents:** bud-factory

**Files changed:**
- `src/app/(app)/dashboard/growth-hq/page.tsx` *(re-edited)*
- `src/app/(app)/dashboard/story-engine/journal/_components/QuickCaptureWidget.tsx` *(re-edited)*
- `src/app/(app)/dashboard/story-engine/opportunities/page.tsx`
- `src/app/api/growth-hq/route.ts` *(re-edited)*
- `src/app/api/journal/quick-capture/route.ts` *(re-edited)*
- `src/app/api/story-drafts/generate/route.ts`
- `src/app/api/story-drafts/suggest/route.ts`
- `src/app/api/story-opportunities/route.ts`
- `src/lib/story/draft-generator.ts`
- `src/types/story-engine.ts`

**Summary:**
tsc, lint, build all clean. Here's what was built:

---

**New files**

- [src/lib/story/draft-generator.ts](src/lib/story/draft-generator.ts) — shared `generateDraftForOpportunity()` helper. Single source of generation logic: loads opportunity context, enforces consent gates, builds the Story Bible prompt, calls Haiku, persists to `story_drafts`. Both routes use it.
- [src/app/api/story-drafts/suggest/route.ts](src/app/api/story-drafts/suggest/route.ts) — `POST /api/story-drafts/suggest`. Takes

> **Re-edit detected** on `src/app/(app)/dashboard/growth-hq/page.tsx`, `src/app/(app)/dashboard/story-engine/journal/_components/QuickCaptureWidget.tsx`, `src/app/api/growth-hq/route.ts`, `src/app/api/journal/quick-capture/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:10] Session 0aeb6d5e

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Files changed:**
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/schema-guard.ts`

**Summary:**
Here's what was built and why each decision was made:

---

## What was built

### [schema-guard.ts](src/lib/bud/schema-guard.ts) — new module (248 lines)

**`extractSchema(src)`** — depth-tracking block reader, not a regex over the full file. It finds `Tables: {`, extracts the block character-by-character tracking brace depth and string literals, then for each table entry jumps the regex cursor past the table's closing `}` before continuing — so `Row:`, `Insert:`, `Update:` inside a table's blo

> **Re-edit detected** on `src/lib/bud/improvement-executor.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:21] Session 0aeb6d5e

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Files changed:**
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/schema-guard.ts`

**Summary:**
Now I have the full picture. Let me write the complete audit.

---

## Agent audit — current state

### Deployment health (unchanged from first audit)

The list is frozen — no new agent PRs since our session started. Our local fixes haven't been committed, so the live executor is still running unfixed code.

```
Total visible:  20 deployments
ERROR:          12  (60%)
READY:           8  (40%)
Newest ERROR:   bud/improve-agent-quote-triage-... (3h ago, same as before)
```

---

### Three structu

> **Re-edit detected** on `src/lib/bud/improvement-executor.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:26] Session 0aeb6d5e

**Task:** <ide_selection>The user selected the lines 1 to 25 from /Users/jacksontaylor/budsatwork/supabase/migrations/105_journal_autofill_internal_filter.sql: -- Phase 6B: Journal quick capture + reviewed story suggestions + internal opportunity filter. alter table public.founder_journal_entries   add column

**Files changed:**
- `src/app/api/webhooks/vercel/route.ts`
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/schema-guard.ts`

**Summary:**
Pushed. Vercel will pick it up now.

Three things shipped:

- **Schema guard** — `src/lib/bud/schema-guard.ts` parses `Database.public.Tables` and blocks patches before they're written if they reference tables or columns that would resolve to `never` at build time
- **Banned-path + TOOLCHAIN_NOTES** — patches targeting `src/agents/` are rejected immediately; the LLM now sees the exact list of 20 valid tables in every prompt
- **Feedback loop fix** — vercel webhook dedup extended to 24h and now i

> **Re-edit detected** on `src/lib/bud/improvement-executor.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:45] Session 33a9b821

**Task:** <ide_selection>The user selected the lines 1 to 17 from /Users/jacksontaylor/budsatwork/supabase/migrations/107_silvan_consent_granted.sql: -- 107_silvan_consent_granted.sql -- Records Silvan's granted consent status. -- Consent obtained and confirmed by Jackson Taylor on 2026-06-06. -- -- Effect: A

**Files changed:**
- `src/app/api/cron/vercel-repair/route.ts` *(re-edited)*
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/repair-quarantine.ts` *(re-edited)*
- `supabase/migrations/109_repair_quarantine.sql`

**Summary:**
All done. Here's what was built:

---

## Files changed

**New files:**
- [supabase/migrations/109_repair_quarantine.sql](supabase/migrations/109_repair_quarantine.sql) — `bud_repair_quarantine` table (branch, commit SHA, deployment ID, failing file/line, error text, source agent, attempt count, status, blocked_until). Unique on `branch`. Service-role-only RLS.
- [src/lib/bud/repair-quarantine.ts](src/lib/bud/repair-quarantine.ts) — quarantine helpers: `checkQuarantine`, `recordFailedAttempt`, `

> **Re-edit detected** on `src/app/api/cron/vercel-repair/route.ts`, `src/lib/bud/improvement-executor.ts`, `src/lib/bud/repair-quarantine.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:51] Session 33a9b821

**Task:** <ide_selection>The user selected the lines 1 to 17 from /Users/jacksontaylor/budsatwork/supabase/migrations/107_silvan_consent_granted.sql: -- 107_silvan_consent_granted.sql -- Records Silvan's granted consent status. -- Consent obtained and confirmed by Jackson Taylor on 2026-06-06. -- -- Effect: A

**Files changed:**
- `src/app/api/cron/vercel-repair/route.ts` *(re-edited)*
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/repair-quarantine.ts` *(re-edited)*
- `supabase/migrations/109_repair_quarantine.sql`

**Summary:**
Migration tracked. All done — here's the full deploy summary:

**Committed** — `03a0e4a` — 37 files, 3,270 insertions  
**Pushed** — `origin/main` — Vercel deployment triggered automatically  
**Migration 109 applied** — `bud_repair_quarantine` table live in production

The quarantine layer is fully operational. Next time a `bud/*` branch fails CI repair, the executor will record it in `bud_repair_quarantine`, and the cron will refuse to re-attempt it within the 24-hour window. After two failure

> **Re-edit detected** on `src/app/api/cron/vercel-repair/route.ts`, `src/lib/bud/improvement-executor.ts`, `src/lib/bud/repair-quarantine.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:15] Session 33a9b821

**Task:** <ide_selection>The user selected the lines 1 to 17 from /Users/jacksontaylor/budsatwork/supabase/migrations/107_silvan_consent_granted.sql: -- 107_silvan_consent_granted.sql -- Records Silvan's granted consent status. -- Consent obtained and confirmed by Jackson Taylor on 2026-06-06. -- -- Effect: A

**Files changed:**
- `src/app/(app)/dashboard/agents/_components/RepairQuarantinePanel.tsx`
- `src/app/(app)/dashboard/agents/page.tsx`
- `src/app/api/cron/vercel-repair/route.ts` *(re-edited)*
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/repair-quarantine.ts` *(re-edited)*
- `supabase/migrations/109_repair_quarantine.sql`

**Summary:**
Done. Here's the summary:

---

## Files changed

- [src/app/(app)/dashboard/agents/_components/RepairQuarantinePanel.tsx](src/app/(app)/dashboard/agents/_components/RepairQuarantinePanel.tsx) — new read-only panel component
- [src/app/(app)/dashboard/agents/page.tsx](src/app/(app)/dashboard/agents/page.tsx) — converted from `redirect()` to a real server page that fetches and renders the panel

## Where it appears

**`/dashboard/agents`** — previously redirected to mission-control, now renders d

> **Re-edit detected** on `src/app/api/cron/vercel-repair/route.ts`, `src/lib/bud/improvement-executor.ts`, `src/lib/bud/repair-quarantine.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:20] Session 33a9b821

**Task:** <ide_selection>The user selected the lines 1 to 17 from /Users/jacksontaylor/budsatwork/supabase/migrations/107_silvan_consent_granted.sql: -- 107_silvan_consent_granted.sql -- Records Silvan's granted consent status. -- Consent obtained and confirmed by Jackson Taylor on 2026-06-06. -- -- Effect: A

**Files changed:**
- `src/app/(app)/dashboard/agents/_components/RepairQuarantinePanel.tsx`
- `src/app/(app)/dashboard/agents/page.tsx`
- `src/app/api/cron/vercel-repair/route.ts` *(re-edited)*
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/repair-quarantine.ts` *(re-edited)*
- `supabase/migrations/109_repair_quarantine.sql`

**Summary:**
Pushed `b28136d`. All changes are live — Vercel will deploy from the latest commit. Nothing else is uncommitted.

> **Re-edit detected** on `src/app/api/cron/vercel-repair/route.ts`, `src/lib/bud/improvement-executor.ts`, `src/lib/bud/repair-quarantine.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [11:03] Session 33a9b821

**Task:** <ide_selection>The user selected the lines 1 to 17 from /Users/jacksontaylor/budsatwork/supabase/migrations/107_silvan_consent_granted.sql: -- 107_silvan_consent_granted.sql -- Records Silvan's granted consent status. -- Consent obtained and confirmed by Jackson Taylor on 2026-06-06. -- -- Effect: A

**Files changed:**
- `src/app/(app)/dashboard/agents/_components/RepairQuarantinePanel.tsx`
- `src/app/(app)/dashboard/agents/page.tsx`
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx` *(re-edited)*
- `src/app/(app)/dashboard/mission-control/_components/RepairQuarantineSection.tsx`
- `src/app/(app)/dashboard/mission-control/page.tsx` *(re-edited)*
- `src/app/api/cron/vercel-repair/route.ts` *(re-edited)*
- `src/lib/bud/improvement-executor.ts` *(re-edited)*
- `src/lib/bud/repair-quarantine.ts` *(re-edited)*
- `supabase/migrations/109_repair_quarantine.sql`

**Summary:**
Pushed `d1323e2`. Vercel is deploying now.

The panel is now at **`/dashboard/mission-control?tab=agents`** — scroll below the agent hierarchy grid to find **Repair Quarantine**. It shows active quarantines (Abandoned in rose, Blocked in amber) followed by Resolved (emerald), with an empty state if none exist. The styling matches the dark slate mission-control shell.

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`, `src/app/(app)/dashboard/mission-control/page.tsx`, `src/app/api/cron/vercel-repair/route.ts`, `src/lib/bud/improvement-executor.ts`, `src/lib/bud/repair-quarantine.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [20:53] Session 86ac8c0f

**Task:** Implement an Executive Agent Layer for Bud OS. Goal: Create AI executives that monitor the business, create decisions, assign tasks to worker agents, measure outcomes, and store learnings. Add: 1. Database migration: - executive_agents - executive_reports - executive_decisions - agent_tasks - agent_

**Dev OS agents:** bud-taste, bud-factory

**Summary:**
All files verified on disk. Here's what was built:

**Executive Agent Layer — complete**

| Area | Files |
|------|-------|
| Migration | `supabase/migrations/110_executive_agents.sql` — 7 new tables |
| Types | `src/lib/agents/executive/types.ts` |
| Agents | `ceo-agent.ts`, `coo-agent.ts`, `cmo-agent.ts`, `cfo-agent.ts`, `chief-of-staff.ts` |
| Cron | `/api/cron/executive-review` (daily), `/api/cron/executive-learning-review` (weekly) |
| Dashboard | `/dashboard/executive` — 4-tab Executive HQ

---
## [21:08] Session 86ac8c0f

**Task:** Implement an Executive Agent Layer for Bud OS. Goal: Create AI executives that monitor the business, create decisions, assign tasks to worker agents, measure outcomes, and store learnings. Add: 1. Database migration: - executive_agents - executive_reports - executive_decisions - agent_tasks - agent_

**Dev OS agents:** bud-taste, bud-factory

**Files changed:**
- `src/app/api/cron/executive-learning-review/route.ts`
- `src/app/api/cron/executive-review/route.ts`
- `supabase/migrations/110_executive_agents.sql`

**Summary:**
Build clean. Audit summary:

---

**3 defects found and patched:**

| # | Defect | Fix |
|---|--------|-----|
| 1 | Migration 110 — all 7 tables had no RLS | Added `enable row level security` + `Service role full access` policy per table, matching the project-wide pattern from 109 |
| 2 | Migration 110 — 4 tables with `updated_at` columns had no trigger | Added `before update` triggers reusing `public.set_updated_at()` already defined in 037 |
| 3 | Both cron routes — `if (cronSecret && ...)` fa

---
## [21:22] Session 86ac8c0f

**Task:** Implement an Executive Agent Layer for Bud OS. Goal: Create AI executives that monitor the business, create decisions, assign tasks to worker agents, measure outcomes, and store learnings. Add: 1. Database migration: - executive_agents - executive_reports - executive_decisions - agent_tasks - agent_

**Dev OS agents:** bud-taste, bud-factory

**Files changed:**
- `src/app/api/cron/executive-learning-review/route.ts`
- `src/app/api/cron/executive-review/route.ts` *(re-edited)*
- `src/lib/agents/agents/ceo-agent.ts`
- `src/lib/agents/agents/cfo-agent.ts`
- `src/lib/agents/agents/chief-of-staff.ts`
- `src/lib/agents/agents/cmo-agent.ts`
- `src/lib/agents/agents/coo-agent.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `src/lib/agents/types.ts`
- `supabase/migrations/110_executive_agents.sql`

**Summary:**
Clean. Everything done. Here's the full status:

---

**1 — Migration 110 applied** ✅ to `ukuhrycxptcbamgacnfm` (buds-at-work). All 7 tables live in production with RLS and triggers.

**2 — Supabase types** — no `database.types.ts` file exists in this project; `ctx.supabase` is untyped `SupabaseClient`. `tsc` was already clean and remains so.

**3 — CRON_SECRET** ✅ confirmed in `.env.local`. Before activating the Vercel cron, verify it's also set in Vercel dashboard → Settings → Environment Vari

> **Re-edit detected** on `src/app/api/cron/executive-review/route.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [21:25] Session 86ac8c0f

**Task:** Implement an Executive Agent Layer for Bud OS. Goal: Create AI executives that monitor the business, create decisions, assign tasks to worker agents, measure outcomes, and store learnings. Add: 1. Database migration: - executive_agents - executive_reports - executive_decisions - agent_tasks - agent_

**Dev OS agents:** bud-taste, bud-factory

**Files changed:**
- `src/app/api/cron/executive-learning-review/route.ts`
- `src/app/api/cron/executive-review/route.ts` *(re-edited)*
- `src/lib/agents/agents/ceo-agent.ts`
- `src/lib/agents/agents/cfo-agent.ts`
- `src/lib/agents/agents/chief-of-staff.ts`
- `src/lib/agents/agents/cmo-agent.ts`
- `src/lib/agents/agents/coo-agent.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `src/lib/agents/types.ts`
- `supabase/migrations/110_executive_agents.sql`

**Summary:**
Deployment confirmed and building.

**`dpl_13QJtbx4gYDuQTn62ht3vrmGs5M6`** — `BUILDING` → production (`budsatwork.com`)
Commit: `0f66f62` — "Add Executive Agent Layer — CEO, COO, CMO, CFO, Chief of Staff"

Vercel will promote to production automatically once the build completes (typically 2–3 minutes). You can track it at the inspector URL Vercel returned.

**Next steps once it's live:**

1. **Dry-run test** — confirm the layer reads your real data correctly:
   ```bash
   curl -H "Authorization

> **Re-edit detected** on `src/app/api/cron/executive-review/route.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [21:33] Session 86ac8c0f

**Task:** Implement an Executive Agent Layer for Bud OS. Goal: Create AI executives that monitor the business, create decisions, assign tasks to worker agents, measure outcomes, and store learnings. Add: 1. Database migration: - executive_agents - executive_reports - executive_decisions - agent_tasks - agent_

**Dev OS agents:** bud-taste, bud-factory

**Files changed:**
- `src/app/api/cron/executive-learning-review/route.ts`
- `src/app/api/cron/executive-review/route.ts` *(re-edited)*
- `src/lib/agents/agents/ceo-agent.ts`
- `src/lib/agents/agents/cfo-agent.ts`
- `src/lib/agents/agents/chief-of-staff.ts`
- `src/lib/agents/agents/cmo-agent.ts`
- `src/lib/agents/agents/coo-agent.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `src/lib/agents/types.ts`
- `supabase/migrations/110_executive_agents.sql`

**Summary:**
All checks complete. Here are the findings:

---

**Local route path:** `src/app/api/cron/executive-review/route.ts`

**Expected URL:** `https://budsatwork.com/api/cron/executive-review`

**Deployed URL:** Confirmed compiled in build output:
```
├ ƒ /api/cron/executive-review   821 B   223 kB
```

**Root cause: timing.** The deployment was in state `BUILDING` when you ran the curl. Vercel served the previous production deployment, which didn't have the route — hence the public 404 page. The depl

> **Re-edit detected** on `src/app/api/cron/executive-review/route.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [21:40] Session 86ac8c0f

**Task:** Implement an Executive Agent Layer for Bud OS. Goal: Create AI executives that monitor the business, create decisions, assign tasks to worker agents, measure outcomes, and store learnings. Add: 1. Database migration: - executive_agents - executive_reports - executive_decisions - agent_tasks - agent_

**Dev OS agents:** bud-taste, bud-factory

**Files changed:**
- `src/app/api/cron/executive-learning-review/route.ts`
- `src/app/api/cron/executive-review/route.ts` *(re-edited)*
- `src/lib/agents/agents/ceo-agent.ts`
- `src/lib/agents/agents/cfo-agent.ts`
- `src/lib/agents/agents/chief-of-staff.ts`
- `src/lib/agents/agents/cmo-agent.ts`
- `src/lib/agents/agents/coo-agent.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `src/lib/agents/types.ts`
- `supabase/migrations/110_executive_agents.sql`
- `supabase/migrations/111_executive_agent_seeds.sql`

**Summary:**
All 5 agents responding correctly. **`ran: 5, failed: 0`**.

The zeroes are expected — the agents are querying the correct tables (`orders`, `leads`, `quotes`, `subscriptions`, `crew_members`) and returning empty metrics. This means either those tables are empty in the current environment, or the table names don't match what's in production.

**What each agent returned:**

| Agent | Status | Metrics |
|-------|--------|---------|
| CEO | ✅ ok | A$0 revenue, 0 jobs, 0 leads |
| COO | ✅ ok | 0/0 c

> **Re-edit detected** on `src/app/api/cron/executive-review/route.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [21:44] Session 86ac8c0f

**Task:** Implement an Executive Agent Layer for Bud OS. Goal: Create AI executives that monitor the business, create decisions, assign tasks to worker agents, measure outcomes, and store learnings. Add: 1. Database migration: - executive_agents - executive_reports - executive_decisions - agent_tasks - agent_

**Dev OS agents:** bud-taste, bud-factory

**Files changed:**
- `src/app/api/cron/executive-learning-review/route.ts`
- `src/app/api/cron/executive-review/route.ts` *(re-edited)*
- `src/lib/agents/agents/ceo-agent.ts`
- `src/lib/agents/agents/cfo-agent.ts`
- `src/lib/agents/agents/chief-of-staff.ts`
- `src/lib/agents/agents/cmo-agent.ts`
- `src/lib/agents/agents/coo-agent.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `src/lib/agents/types.ts`
- `supabase/migrations/110_executive_agents.sql`
- `supabase/migrations/111_executive_agent_seeds.sql`

**Summary:**
Tables exist and have real data — but in the 8–30 day range, not the last 7 days. The agents are working correctly: they query `created_at >= now() - 7 days` and the 1 order, 6 leads, and 3 quotes are all older than that window.

**Diagnosis: correct behaviour, not a bug.** The zeroes mean "quiet past 7 days", not "broken queries."

When business activity is running normally (orders, leads, quotes within 7 days), the executives will surface real metrics and generate decisions. The dry-run pipeli

> **Re-edit detected** on `src/app/api/cron/executive-review/route.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [21:45] Session 86ac8c0f

**Task:** Implement an Executive Agent Layer for Bud OS. Goal: Create AI executives that monitor the business, create decisions, assign tasks to worker agents, measure outcomes, and store learnings. Add: 1. Database migration: - executive_agents - executive_reports - executive_decisions - agent_tasks - agent_

**Dev OS agents:** bud-taste, bud-factory

**Files changed:**
- `src/app/api/cron/executive-learning-review/route.ts`
- `src/app/api/cron/executive-review/route.ts` *(re-edited)*
- `src/lib/agents/agents/ceo-agent.ts`
- `src/lib/agents/agents/cfo-agent.ts`
- `src/lib/agents/agents/chief-of-staff.ts`
- `src/lib/agents/agents/cmo-agent.ts`
- `src/lib/agents/agents/coo-agent.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `src/lib/agents/types.ts`
- `supabase/migrations/110_executive_agents.sql`
- `supabase/migrations/111_executive_agent_seeds.sql`

**Summary:**
Already done. Both commits are pushed and deployed.

**Latest production deployment:** `dpl_6Hd879qgGG2g8nyVQGbAKYigC6ed` — `READY` → `budsatwork.com`
Commit: `5ee33a6` — "Seed executive agents and extend category constraint"

**Full deployment history for this session:**

| Commit | Deployment | Status |
|--------|-----------|--------|
| `5ee33a6` Seed executive agents | `dpl_6Hd...` | ✅ READY → production |
| `0f66f62` Add Executive Agent Layer | `dpl_13Q...` | ✅ READY → production |

**Curren

> **Re-edit detected** on `src/app/api/cron/executive-review/route.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [22:31] Session 657993c4

**Task:** <ide_selection>The user selected the lines 1 to 48 from /Users/jacksontaylor/budsatwork/supabase/migrations/111_executive_agent_seeds.sql: -- 111_executive_agent_seeds.sql -- Register the 5 executive agents so runAgent() can find them. -- -- Step 1: Extend the agents.category check constraint to inc

**Files changed:**
- `vercel.json`

**Summary:**
Done. Added both crons with 5-minute offsets to stagger them from the existing slots:

- `executive-review` → `5 20 * * *` (daily 20:05, offset from `field-producer`)
- `executive-learning-review` → `5 21 * * 0` (Sundays 21:05, offset from `weekly-kpi-email`)

Push when ready — Vercel will pick them up on next deploy.

---
## [22:33] Session 657993c4

**Task:** <ide_selection>The user selected the lines 1 to 48 from /Users/jacksontaylor/budsatwork/supabase/migrations/111_executive_agent_seeds.sql: -- 111_executive_agent_seeds.sql -- Register the 5 executive agents so runAgent() can find them. -- -- Step 1: Extend the agents.category check constraint to inc

**Files changed:**
- `vercel.json`

**Summary:**
Pushed. Vercel will pick up the new crons on deploy — `executive-review` fires daily at 20:05 and `executive-learning-review` every Sunday at 21:05.

---
## [22:41] Session 9e819417

**Task:** <ide_selection>The user selected the lines 1 to 48 from /Users/jacksontaylor/budsatwork/supabase/migrations/111_executive_agent_seeds.sql: -- 111_executive_agent_seeds.sql -- Register the 5 executive agents so runAgent() can find them. -- -- Step 1: Extend the agents.category check constraint to inc

**Summary:**
**Deployed successfully.** Production is live at [budsatwork.com](https://budsatwork.com).

Here's a summary of what the bots added across the last 6 commits:

**Executive Agent Layer** (commits `0f66f62` → `134b654`)
- 5 new agents: CEO, COO, CMO, CFO, Chief of Staff — all `autonomy: 'review'` (no auto-execution without human approval)
- 2 new cron routes: `executive-review` (daily 8:05 PM AEST) and `executive-learning-review` (weekly Sunday)
- 7 new Supabase tables for decisions, tasks, metric

---
## [22:46] Session 16939cc6

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> Audit executive-agent data inputs. Current issue: Executive agents only see operational and financial metrics. They are blind to: - G

**Summary:**
I have everything I need. Here is the full audit.

---

## Executive Agent Intelligence Audit

---

### CEO Agent

**Currently reads**
- `orders` (7d + 30d trend): revenue, job count
- `leads` (7d): lead volume
- `quotes` (7d): conversion rate
- `subscriptions`: MRR

**What it is blind to**
- Content pipeline: how many ideas, scripts, and production cards are in flight
- Active marketing campaigns and their channels
- Story opportunities with high `story_score` waiting to be actioned
- AI-genera

---
## [22:54] Session 16939cc6

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> Audit executive-agent data inputs. Current issue: Executive agents only see operational and financial metrics. They are blind to: - G

**Files changed:**
- `src/lib/agents/agents/ceo-agent.ts` *(re-edited)*
- `src/lib/agents/agents/cmo-agent.ts`

**Summary:**
Clean — typecheck, lint, and build all pass. Here's what was implemented:

---

### Phase 1 — CMO Agent

**New queries added (10 total, up from 4):**
- `story_opportunities` — count `new`, max `story_score`
- `story_drafts` — AI drafts in `draft` status (last 48h)
- `founder_journal_entries` — has entry today
- `content_ideas` — all non-archived, by status
- `content_scripts` — all non-archived, by status
- `content_production_cards` — all non-published, by status
- `marketing_publishing_queue` 

> **Re-edit detected** on `src/lib/agents/agents/ceo-agent.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [23:20] Session 16939cc6

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> Audit executive-agent data inputs. Current issue: Executive agents only see operational and financial metrics. They are blind to: - G

**Files changed:**
- `src/lib/agents/agents/ceo-agent.ts` *(re-edited)*
- `src/lib/agents/agents/cmo-agent.ts` *(re-edited)*

**Summary:**
Build clean. Here's the verified status:

---

### What was wrong

Three bugs working together:

1. **`AgentOutputSchema` requires `summary` inside `output`** — not just on the result object. Our output objects were missing it, so `safeParse` failed every time, and the runtime normaliser ran — replacing the output with a stripped shell and burying everything in `raw_output`.

2. **Schema normalization is output-destructive but findings-preserving** — it was smart enough to copy our `findings` st

> **Re-edit detected** on `src/lib/agents/agents/ceo-agent.ts`, `src/lib/agents/agents/cmo-agent.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

> **ADR signal detected.** Consider running `npx tsx scripts/vault-adr.ts` to record this decision formally.

---
