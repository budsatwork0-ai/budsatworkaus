# Dev Log 2026-06-05

## [09:35] Session 42907da2

**Task:** <ide_selection>The user selected the lines 1 to 62 from /Users/jacksontaylor/budsatwork/supabase/migrations/093_story_drafts.sql: -- Phase 2F: Story Drafts -- Stores AI-generated and manually edited content drafts linked to Story Opportunities. -- Admin-only. No auto-publishing. All drafts require h

**Dev OS agents:** bud-factory

**Files changed:**
- `src/app/(app)/dashboard/story-engine/opportunities/page.tsx` *(re-edited)*
- `src/app/api/story-opportunities/score/route.ts`
- `src/app/api/story-reviews/[id]/route.ts`
- `src/app/api/story-reviews/run/route.ts`
- `src/lib/story/opportunity-scoring.ts`
- `src/types/story-engine.ts`
- `src/types/story-review.ts`
- `supabase/migrations/094_story_reviews.sql`
- `supabase/migrations/095_story_opportunity_scoring.sql`

**Summary:**
Now I have everything I need. The migration hasn't been applied yet so there are no live scores — but I have the actual opportunity data and the engine code, which means I can score both opportunities precisely by hand and audit the logic directly. This is actually more rigorous than querying stored results.

---

## Phase 2D Review — Story Scoring Audit

### Dataset reality

The database contains **2 opportunities**, both auto-detected milestones. This is the most important finding: a top-25 au

> **Re-edit detected** on `src/app/(app)/dashboard/story-engine/opportunities/page.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:16] Session 2f19f854

**Task:** Phase 4C Audit — Distribution Playbooks Do not build new features. Audit: - Playbooks can be created, edited, deleted. - Seeded playbooks exist. - Steps and checklist fields behave correctly. - Optional campaign linking works safely. - Archived playbooks are handled correctly. - No social posting ex

**Files changed:**
- `src/app/(app)/dashboard/marketing/playbooks/page.tsx` *(re-edited)*

**Summary:**
---

## Phase 4C Audit — Distribution Playbooks

### Defects Found & Fixed

**D1 — Duplicate React key in `ListBlock`** (fixed)
`key={item}` would silently produce React duplicate-key warnings if two step or checklist entries had identical text. Changed to `key={i}` (index).

**D2 — `<ol>` rendered without numbering** (fixed)
`ListBlock` used `<ol>` semantically but no list-style CSS was applied, so steps displayed as a flat unnumbered list. Added `list-decimal list-inside` — steps now render wi

> **Re-edit detected** on `src/app/(app)/dashboard/marketing/playbooks/page.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:30] Session 2f19f854

**Task:** Phase 4C Audit — Distribution Playbooks Do not build new features. Audit: - Playbooks can be created, edited, deleted. - Seeded playbooks exist. - Steps and checklist fields behave correctly. - Optional campaign linking works safely. - Archived playbooks are handled correctly. - No social posting ex

**Files changed:**
- `src/app/(app)/dashboard/layout.tsx`
- `src/app/(app)/dashboard/marketing/page.tsx` *(re-edited)*
- `src/app/(app)/dashboard/marketing/playbooks/page.tsx` *(re-edited)*
- `src/app/(app)/dashboard/marketing/social-channels/page.tsx`
- `src/app/api/social-channels/[id]/route.ts`
- `src/app/api/social-channels/route.ts`
- `src/types/social-channel.ts`
- `supabase/migrations/103_social_channels.sql`

**Summary:**
---

## Phase 4D — Social Channel Profiles — Done

**Files created:**

| File | Purpose |
|---|---|
| [supabase/migrations/103_social_channels.sql](supabase/migrations/103_social_channels.sql) | Table, RLS, indexes, updated_at trigger, 6 seeded channels |
| [src/types/social-channel.ts](src/types/social-channel.ts) | `SocialChannel`, `SocialChannelStatus`, `SocialChannelPlatform`, status styles, platform labels |
| [src/app/api/social-channels/route.ts](src/app/api/social-channels/route.ts) | GE

> **Re-edit detected** on `src/app/(app)/dashboard/marketing/page.tsx`, `src/app/(app)/dashboard/marketing/playbooks/page.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:37] Session 2f19f854

**Task:** Phase 4C Audit — Distribution Playbooks Do not build new features. Audit: - Playbooks can be created, edited, deleted. - Seeded playbooks exist. - Steps and checklist fields behave correctly. - Optional campaign linking works safely. - Archived playbooks are handled correctly. - No social posting ex

**Files changed:**
- `src/app/(app)/dashboard/layout.tsx`
- `src/app/(app)/dashboard/marketing/page.tsx` *(re-edited)*
- `src/app/(app)/dashboard/marketing/playbooks/page.tsx` *(re-edited)*
- `src/app/(app)/dashboard/marketing/social-channels/page.tsx` *(re-edited)*
- `src/app/api/social-channels/[id]/route.ts`
- `src/app/api/social-channels/route.ts`
- `src/types/social-channel.ts`
- `supabase/migrations/103_social_channels.sql`

**Summary:**
Clean. Here is the full audit result.

---

## Phase 4D Audit — Social Channel Profiles

### Defect Found & Fixed

**D1 — Double `@` in handle display** (fixed)
Form placeholder says `@username`, so a user entering `@budsatwork` stores that value. The card display at [page.tsx:245](src/app/(app)/dashboard/marketing/social-channels/page.tsx#L245) prepends its own `@`, producing `@@budsatwork`. Fixed in `toPayload`: `draft.handle.trim().replace(/^@+/, '')` — handles are now always stored without t

> **Re-edit detected** on `src/app/(app)/dashboard/marketing/page.tsx`, `src/app/(app)/dashboard/marketing/playbooks/page.tsx`, `src/app/(app)/dashboard/marketing/social-channels/page.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:48] Session 07757049

**Task:** <ide_selection>The user selected the lines 1 to 57 from /Users/jacksontaylor/budsatwork/supabase/migrations/103_social_channels.sql: -- Phase 4D: Social Channel Profiles -- Manual profile management only. No OAuth, no platform API calls, no posting, -- no analytics fetching, no AI captions, no agent

**Files changed:**
- `src/app/(app)/dashboard/research-lab/page.tsx`
- `src/app/(app)/dashboard/research-lab/trends/page.tsx`
- `src/app/api/research-trends/[id]/route.ts`
- `src/app/api/research-trends/route.ts`
- `src/types/research-trend.ts`
- `supabase/migrations/104_research_trends.sql`

**Summary:**
Phase 5A is complete and building clean. Here's what was built:

**Migration** — [104_research_trends.sql](supabase/migrations/104_research_trends.sql)
- `research_trends` table with all required fields, CHECK constraints, RLS (admin/owner only), 5 indexes, updated_at trigger

**Type** — [src/types/research-trend.ts](src/types/research-trend.ts)
- All enums as `as const` arrays, full `ResearchTrend` interface, label maps and colour styles for platform/type/urgency/status

**API** — [/api/researc

---
## [13:02] Session 07757049

**Task:** <ide_selection>The user selected the lines 1 to 57 from /Users/jacksontaylor/budsatwork/supabase/migrations/103_social_channels.sql: -- Phase 4D: Social Channel Profiles -- Manual profile management only. No OAuth, no platform API calls, no posting, -- no analytics fetching, no AI captions, no agent

**Files changed:**
- `src/app/(app)/dashboard/research-lab/page.tsx`
- `src/app/(app)/dashboard/research-lab/trends/page.tsx` *(re-edited)*
- `src/app/api/research-trends/[id]/route.ts` *(re-edited)*
- `src/app/api/research-trends/route.ts` *(re-edited)*
- `src/types/research-trend.ts`
- `supabase/migrations/104_research_trends.sql`

**Summary:**
Clean. Here is the full audit report.

---

## Phase 5A Audit — Verdict

### Remote DB

| Check | Result |
|---|---|
| Table exists | ✓ |
| All 12 columns correct types/defaults | ✓ |
| RLS enabled | ✓ |
| Policy: ALL ops, admin/owner, permissive | ✓ |
| CHECK: platform, trend_type, urgency, status | ✓ |
| Indexes: platform, status, urgency, trend_type, created_at | ✓ |

### API routes

| Check | Result |
|---|---|
| GET filters (platform, status, trend_type, urgency) | ✓ Server-side enum valida

> **Re-edit detected** on `src/app/(app)/dashboard/research-lab/trends/page.tsx`, `src/app/api/research-trends/[id]/route.ts`, `src/app/api/research-trends/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [13:07] Session 07757049

**Task:** <ide_selection>The user selected the lines 1 to 57 from /Users/jacksontaylor/budsatwork/supabase/migrations/103_social_channels.sql: -- Phase 4D: Social Channel Profiles -- Manual profile management only. No OAuth, no platform API calls, no posting, -- no analytics fetching, no AI captions, no agent

**Files changed:**
- `src/app/(app)/dashboard/research-lab/page.tsx`
- `src/app/(app)/dashboard/research-lab/trends/page.tsx` *(re-edited)*
- `src/app/api/research-trends/[id]/route.ts` *(re-edited)*
- `src/app/api/research-trends/route.ts` *(re-edited)*
- `src/types/research-trend.ts`
- `supabase/migrations/104_research_trends.sql`

**Summary:**
Deployment is confirmed live. Here's the status:

**Commit pushed:** `9ee5c42` → `origin/main` ✓

**Vercel production build:** `BUILDING` — triggered automatically from the push, targeting production. Inspector: [budsatworkaus/587LFkytF](https://vercel.com/budsatwork0-ais-projects/budsatworkaus/587LFkytFUvUEQNxCBLeRgQxLL6V)

The previous successful production deployment was the Story Engine Phase 2H.5 commit. This one (56 files, 10,562 insertions) is now building on top of it. It should complete

> **Re-edit detected** on `src/app/(app)/dashboard/research-lab/trends/page.tsx`, `src/app/api/research-trends/[id]/route.ts`, `src/app/api/research-trends/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [13:33] Session 5c0d61ac

**Task:** <ide_selection>The user selected the lines 1 to 50 from /Users/jacksontaylor/budsatwork/supabase/migrations/104_research_trends.sql: -- Phase 5A: Research Lab — Trend Intelligence -- Manual trend entry only. No scraping, no external API calls, no AI generation, -- no agents, and no automations. Tren

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/growth-hq/page.tsx`
- `src/app/api/growth-hq/route.ts`

**Summary:**
Both routes built cleanly. Here's what was delivered:

---

**`/api/growth-hq`** — single aggregation endpoint, 10 parallel Supabase queries:
- Active chapter, top-scored opportunity, pipeline counts (ideas/scripts/production/queue), active campaigns, leads (7-day window), watching/adapting trends, today's journal entry

**`/dashboard/growth-hq`** — live command centre replacing the static placeholder:

| Section | Source | Links to |
|---|---|---|
| Current Chapter | `story_chapters` | Story En

> **ADR signal detected.** Consider running `npx tsx scripts/vault-adr.ts` to record this decision formally.

---
## [13:39] Session 5c0d61ac

**Task:** <ide_selection>The user selected the lines 1 to 50 from /Users/jacksontaylor/budsatwork/supabase/migrations/104_research_trends.sql: -- Phase 5A: Research Lab — Trend Intelligence -- Manual trend entry only. No scraping, no external API calls, no AI generation, -- no agents, and no automations. Tren

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/growth-hq/page.tsx` *(re-edited)*
- `src/app/api/growth-hq/route.ts` *(re-edited)*

**Summary:**
Clean. Typecheck and build both pass with no errors.

---

## Phase 6A Audit — Verdict

**Defects found: 2**

| # | Defect | Severity | Status |
|---|--------|----------|--------|
| 1 | `today` computed in UTC — journal check wrong from midnight–10am AEST | Minor | Fixed — now uses `Intl.DateTimeFormat` with `Australia/Brisbane` |
| 2 | `bg-black/8` non-standard opacity class, not in Tailwind default scale | Trivial | Fixed — changed to `bg-black/10` |
| 3 | Redundant JS filter on production cou

> **Re-edit detected** on `src/app/(app)/dashboard/growth-hq/page.tsx`, `src/app/api/growth-hq/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [13:49] Session 5c0d61ac

**Task:** <ide_selection>The user selected the lines 1 to 50 from /Users/jacksontaylor/budsatwork/supabase/migrations/104_research_trends.sql: -- Phase 5A: Research Lab — Trend Intelligence -- Manual trend entry only. No scraping, no external API calls, no AI generation, -- no agents, and no automations. Tren

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/growth-hq/page.tsx` *(re-edited)*
- `src/app/(app)/dashboard/story-engine/journal/_components/JournalEntryForm.tsx`
- `src/app/api/growth-hq/route.ts` *(re-edited)*

**Summary:**
Clean. Typecheck and build both pass.

---

## Growth & Marketing End-to-End Smoke Test

### Workflow chain — pass/fail

| Step | Check | Result |
|---|---|---|
| **1. Journal entry** | Admin-gated POST (`/api/journal`) | ✅ Pass |
| | Form submits all text fields correctly | ✅ Pass |
| | `todayDate()` was using UTC — wrong default date midnight–10am AEST | **Fixed** |
| | No private text exposed in API response (admin-only) | ✅ Pass |
| **2. Story Opportunity** | Detect route selects zero privat

> **Re-edit detected** on `src/app/(app)/dashboard/growth-hq/page.tsx`, `src/app/api/growth-hq/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [16:20] Session 5c0d61ac

**Task:** <ide_selection>The user selected the lines 1 to 50 from /Users/jacksontaylor/budsatwork/supabase/migrations/104_research_trends.sql: -- Phase 5A: Research Lab — Trend Intelligence -- Manual trend entry only. No scraping, no external API calls, no AI generation, -- no agents, and no automations. Tren

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/growth-hq/page.tsx` *(re-edited)*
- `src/app/(app)/dashboard/story-engine/journal/_components/JournalEntryForm.tsx`
- `src/app/api/growth-hq/route.ts` *(re-edited)*

**Summary:**
Deployed. The most recent production deployment is **READY**:

- **Commit:** `6ee7619` — "Add Growth HQ Phase 6A — command centre + timezone fixes"
- **State:** `READY` (production)
- **URL:** `budsatworkaus-ep8f9w28p-budsatwork0-ais-projects.vercel.app`
- **Inspector:** https://vercel.com/budsatwork0-ais-projects/budsatworkaus/4jRrtHXKasFbbtwzCdUYQJwMYKNA

Growth HQ is live in production at `/dashboard/growth-hq`.

> **Re-edit detected** on `src/app/(app)/dashboard/growth-hq/page.tsx`, `src/app/api/growth-hq/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
