# Dev Log 2026-06-12

## [01:23] Session 28f87adf

**Task:** <ide_selection>The user selected the lines 1 to 77 from /Users/jacksontaylor/budsatwork/supabase/migrations/127_agent_intelligence_actionable_counts.sql: -- Agent Intelligence v2: count only actionable approval work in initiative metrics. update public.bud_root_cause_initiatives i set approval_count

**Summary:**
All clean. Here's the final summary:

---

## Final Report

### Files Renamed (57 renames)

All local files from `023_` through `127_` that had a matching remote history entry were renamed to `<version>_<name>.sql`. Examples:
- `023_payouts.sql` → `20260416004709_023_payouts.sql`
- `127_agent_intelligence_actionable_counts.sql` → `20260611152800_127_agent_intelligence_actionable_counts.sql`

### Stub Files Created (11 stubs)

For remote history entries that had no local file (applied directly wi

---
## [01:28] Session 28f87adf

**Task:** <ide_selection>The user selected the lines 1 to 77 from /Users/jacksontaylor/budsatwork/supabase/migrations/127_agent_intelligence_actionable_counts.sql: -- Agent Intelligence v2: count only actionable approval work in initiative metrics. update public.bud_root_cause_initiatives i set approval_count

**Summary:**
Clean. Only 123 is pending.

**What happened:** You applied migration 122 directly (bypassing `db push`), which is why the table exists but wasn't tracked. I've repaired that.

**To apply 123 correctly** — run this once and it will apply the SQL AND track it automatically:

```bash
npx supabase db push --linked
```

That's it. No more manual SQL files. From now on, `db push` is the correct path for every new migration.

---
## [01:29] Session 28f87adf

**Task:** <ide_selection>The user selected the lines 1 to 77 from /Users/jacksontaylor/budsatwork/supabase/migrations/127_agent_intelligence_actionable_counts.sql: -- Agent Intelligence v2: count only actionable approval work in initiative metrics. update public.bud_root_cause_initiatives i set approval_count

**Summary:**
Both 122 and 123 are tracked. History is clean. `supabase db push --linked` is now the correct command for every future migration — write the file, run push, done.

---
## [01:30] Session 28f87adf

**Task:** <ide_selection>The user selected the lines 1 to 77 from /Users/jacksontaylor/budsatwork/supabase/migrations/127_agent_intelligence_actionable_counts.sql: -- Agent Intelligence v2: count only actionable approval work in initiative metrics. update public.bud_root_cause_initiatives i set approval_count

**Summary:**
Done. Commit `a5d3972` pushed to `main`. Vercel will auto-deploy from here — no changes to app code so the build will be fast. The migration history is fully reconciled and `supabase db push --linked` is the correct path for every future migration.

---
## [10:23] Session 28f87adf

**Task:** <ide_selection>The user selected the lines 1 to 77 from /Users/jacksontaylor/budsatwork/supabase/migrations/127_agent_intelligence_actionable_counts.sql: -- Agent Intelligence v2: count only actionable approval work in initiative metrics. update public.bud_root_cause_initiatives i set approval_count

**Summary:**
---

## Final Report

**Migrations 122 and 123 were already applied and tracked** — completed earlier this session. Nothing to push.

### Dry-Run Result
`Remote database is up to date.` — zero pending migrations. ✓

### Migration History
| version | name |
|---|---|
| `20260611160100` | `123_reviewer_safety_guardrails` |
| `20260611160000` | `122_pr_learning_loop` |
| `20260611152800` | `127_agent_intelligence_actionable_counts` |
| `20260611152600` | `126_agent_intelligence_visible_initiatives`

---
## [11:30] Session 500a52c7

**Task:** Create 10 realistic test customers. Create: - Leads - Quotes - Conversations - Jobs - Reviews Mark all records: is_test = true Ensure: - Dashboards can include or exclude test data - Agent workflows run normally - Business reporting excludes test data by default

**Files changed:**
- `src/app/api/dashboard/route.ts` *(re-edited)*
- `supabase/migrations/20260612100000_128_test_data_columns_and_propagation.sql`
- `supabase/migrations/20260612100100_129_seed_test_customers.sql`

**Summary:**
Done. Here's what was built:

---

**Schema (migration 128)**
- `is_test boolean NOT NULL DEFAULT false` added to `customers`, `quotes`, `orders`, `leads`, `lead_conversations`, `conversations`, `messages`, `ratings`
- Partial indexes on each table for fast `WHERE is_test = true` filtering
- 3 propagation triggers: `lead_conversation` inherits from `lead`, `order` inherits from `quote`, `message` inherits from `conversation`

**Seed data (migration 129 + live in Supabase)**

| Table | Records |


> **Re-edit detected** on `src/app/api/dashboard/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:03] Session 7494dc64

**Task:** Audit the new Bud OS Sandbox Environment for isolation and safety. Context: A Sandbox Environment vertical slice was implemented with: - environment = 'production' | 'sandbox' - sandbox dashboard page - /api/sandbox route - dashboard environment filtering - production approval APIs rejecting sandbox

**Files changed:**
- `src/app/api/agents/actions/[id]/route.ts`

**Summary:**
---

### 5. Mission Control Safety — PASS

All queries in [mission-control/page.tsx](src/app/(app)/dashboard/mission-control/page.tsx) that touch sandboxable tables use explicit `environment = 'production'` filters:

| Query | Filter |
|---|---|
| `agent_runs` | `.eq('environment', 'production')` ✅ |
| `v_pending_agent_actions` | View-level `WHERE environment='production'` (after migration) ✅ |
| `agent_actions` (impact) | `.eq('environment', 'production')` ✅ |
| `agent_runs` (impact) | `.eq('en

---
## [12:20] Session 7494dc64

**Task:** Audit the new Bud OS Sandbox Environment for isolation and safety. Context: A Sandbox Environment vertical slice was implemented with: - environment = 'production' | 'sandbox' - sandbox dashboard page - /api/sandbox route - dashboard environment filtering - production approval APIs rejecting sandbox

**Files changed:**
- `src/app/api/agents/actions/[id]/route.ts`

**Summary:**
Both migrations applied and verified. Here's the final state:

| Table | Production | Sandbox |
|---|---|---|
| `customers` | 7 | 10 (seeded by 129, promoted by 130) |
| `orders` | 1 | 7 (promoted by 130) |
| `quotes` | 3 | 10 (seeded + promoted) |
| `agent_actions` | 79 | 0 |

Production counts are unchanged. All `is_test=true` rows from before migration 130 are now correctly labelled `environment='sandbox'`. The views (`v_pending_agent_actions`, `v_bud_approval_truth`, `v_agent_intelligence_qu

---
## [12:47] Session 6f7edfdd

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/20260612100000_128_test_data_columns_and_propagation.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> push all changes and deploy: Finish deployment and smoke-test the B

**Summary:**
Deployment is already **READY** in production. Here's the full report.

---

## Final Smoke-Test Report

### 1. TypeScript
**PASS** — `npx tsc --noEmit` returned zero errors.

### 2. Build
**PASS** — `npm run build` completed clean. All 14 changed files compiled without warnings or errors.

### 3. Dashboard API environment filtering
**PASS** — Code review of [src/app/api/dashboard/route.ts](src/app/api/dashboard/route.ts):
- Default (no param) → `environment.eq.production`
- `?environment=produc

---

## Agent Training Arena — Implementation Complete

**Migration:** `supabase/migrations/20260612110000_131_agent_training_arena.sql`
5 new tables, all `environment='sandbox'` by default:
- `sandbox_scenarios` — 40 seeded scenario definitions across 7 categories
- `sandbox_training_runs` — per-run execution log
- `sandbox_agent_responses` — captured proposed actions, token counts, costs
- `sandbox_decision_scores` — precision/recall/F1 per run
- `sandbox_lessons_learned` — auto-generated from low-scoring runs (F1 < 0.5)

**Backend:**
- `src/lib/sandbox/scenarios.ts` — TypeScript scenario catalogue (40 templates)
- `src/lib/sandbox/arena.ts` — `runSandboxScenario()` wraps agent `run()` with a sandbox interceptor: `proposeAction` captures actions but never dispatches them. No `resend.emails.send`, no `stripe.*`, no Twilio calls reach production systems.

**API routes (all admin-gated):**
- `POST /api/sandbox/run-scenario` — single scenario by slug
- `POST /api/sandbox/run-pack` — filtered batch (category/difficulty/slugs, max 20)
- `POST /api/sandbox/replay` — re-run a historical training run by ID
- `GET /api/sandbox/leaderboard` — per-agent F1/precision/recall aggregates
- `GET /api/sandbox/lessons` — lessons learned from low-scoring runs
- `GET /api/sandbox/readiness` — fleet readiness % (F1 ≥ 0.5 pass rate)

**UI:** `src/app/(app)/dashboard/sandbox/page.tsx`
Replaced 7 quick-action buttons with Arena UI:
- 4 arena action buttons (Run Customer Pack, Run Ops Pack, Run Finance Pack, Stress Test)
- 40-scenario catalogue with category/difficulty filters and per-scenario Run buttons
- Tab nav: Run Scenarios / Leaderboard / Lessons / Readiness
- Last run result card (F1, precision, recall, proposed actions, cost, duration)
- Existing status/metric cards preserved

**QA:** `tsc --noEmit` clean, `next build` passed (196/196 pages), production isolation grep confirmed — no real calls to `resend.emails.send`, `stripe.*`, or Twilio in sandbox code paths.
## [13:15] Session 3b681756

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/20260612100000_128_test_data_columns_and_propagation.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Upgrade the Buds At Work Sandbox into a realtime Agent Training Are

**Dev OS agents:** bud-factory

**Summary:**
Everything checks out. Here's what was built and verified:

---

## Agent Training Arena — Delivered

### New files

| File | Purpose |
|---|---|
| [supabase/migrations/20260612110000_131_agent_training_arena.sql](supabase/migrations/20260612110000_131_agent_training_arena.sql) | 5 new tables + 40 seeded scenario definitions |
| [src/lib/sandbox/arena.ts](src/lib/sandbox/arena.ts) | Sandbox interceptor, scenario runner, scoring engine, leaderboard query |
| [src/lib/sandbox/scenarios.ts](src/lib

---
## [22:31] Session 6c724988

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/20260612100000_128_test_data_columns_and_propagation.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Apply and smoke test the new Agent Training Arena. Context: The Age

**Files changed:**
- `src/lib/sandbox/arena.ts`

**Summary:**
All checks complete. Here's the full smoke test report:

---

## Agent Training Arena — Smoke Test Report

### Migration Result
**Migration 131 applied successfully.** All 5 tables created, 39 scenarios seeded across 7 categories (customer×8, ops×8, finance×6, growth×5, ndis×4, participant×4, marketplace×4).

---

### Scenario Test Results

**Participant scenario — `ndis-compliance-expiry`**
- Agent: `ndis-compliance`
- Simulated proposed: `[flag_for_review]` (missed `send_email`)
- Expected: `[

---
