# Dev Log 2026-06-01

## [00:07] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
TikTok is the same concept but a bit more involved. Here's the lay of the land:

**What's different from Meta:**
- TikTok requires an **app review** before it can access live account data — this can take 1–5 business days
- OAuth tokens expire and need refreshing (unlike Meta's permanent Page token)
- The API is through **TikTok for Business** (business-api.tiktok.com)

**The steps at a high level:**
1. Create a TikTok developer account at **developers.tiktok.com**
2. Create an app, add the **Bu

---
## [00:29] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
1. Go to **developers.tiktok.com** and log in with your TikTok account

2. Click **Manage apps** (top right) → **Create app**

3. Fill in the details:
   - App name: `Buds At Work Metrics`
   - App type: **Web**
   - Category: **Business**
   - Description: something like "Internal analytics tracking for business account"

4. Once the app is created, you'll land on the app dashboard. Look for a section called **Products** or **Add products**

5. Find **Business Access API** and click **Add** (it

---
## [00:30] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Both pages exist. Use:

- **Terms of Service URL:** `https://budsatworkaus.vercel.app/terms`
- **Privacy Policy URL:** `https://budsatworkaus.vercel.app/privacy`

Fill those in and continue.

---
## [00:31] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
TikTok wants to confirm you own the domain. Click **Verify URL properties** and it'll show you a verification method — usually one of:

1. **HTML file** — download a file and add it to your project
2. **Meta tag** — add a `<meta>` tag to your homepage `<head>`
3. **DNS record** — add a TXT record to your domain

Screenshot which options it gives you and I'll set it up.

---
## [00:32] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
TikTok wants to verify you own all three domains. Click **Verify URL properties** next to the Terms of Service URL first — that'll open Google Search Console or a similar flow.

The quickest verification method is a **meta tag** in your homepage `<head>`. Click **Verify URL properties** and tell me what verification options it shows (HTML file, meta tag, DNS, etc.) and I'll add it to the codebase immediately.

---
## [00:33] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Click **Verify URL properties** next to the Terms of Service URL and screenshot what options it gives you — I need to see the verification method (meta tag, HTML file, or DNS) before I can make any changes.

---
## [00:33] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Select **Domain** (DNS record) — this verifies `budsatwork.com` once and covers all URLs including `/terms`, `/privacy`, and the Vercel subdomain.

Click it and screenshot the next screen — it'll give you a TXT record to add to your DNS.

---
## [00:34] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
You need to add this TXT record to your DNS. Where is `budsatwork.com` registered — is it **GoDaddy**, **Cloudflare**, **Namecheap**, or somewhere else?

The TXT value to add is:
```
tiktok-developers-site-verification=upz3xclo1uIr3kYdADdzwsj25E
```

Once I know your DNS provider I'll tell you exactly where to paste it.

---
## [00:38] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Looks like you're in GoDaddy's DNS editor. Fill in the TXT record like this:

- **Host:** `@`
- **Value:** `tiktok-developers-site-verification=upz3xclo1uIr3kYdADdzwsj25E`
- **TTL:** Automatic (leave as is)

Click the tick/checkmark to save it, then go back to TikTok and click **Verify**. DNS can take a few minutes to an hour to propagate — if it fails immediately, wait 30 minutes and try again.

---
## [00:48] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Fill it in like this:

> Buds At Work is a local home services business (cleaning, yard care, window cleaning, car detailing) based in Brisbane, Australia. This app is used internally to automatically pull our own TikTok account's follower count, video views, and engagement metrics into our business dashboard scoreboard. No user data is collected or stored. No content is posted on behalf of users. The app only reads analytics from our own TikTok business account to replace manual data entry.

Th

---
## [00:50] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
This is the hard part — TikTok requires a demo video of the actual working integration before they'll approve it, which means you need to build the code first, test it in sandbox, then record a screen capture.

Here's the honest situation:

**What's needed to record this video:**
1. I build the TikTok integration code (OAuth flow + metrics fetching)
2. You connect it to TikTok's sandbox environment in the developer portal
3. You screen-record the OAuth login flow and the metrics appearing in the

---
## [00:55] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Fill it in like this:

> We are a small local home services business in Brisbane, Australia. We want to display our own TikTok account's follower count, video views, and engagement metrics on our internal business dashboard so our team can track marketing performance without manually checking the app each day. This is a read-only internal tool — no content is posted, no user data is collected, and the integration only accesses our own TikTok business account.

---
## [00:55] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Try this:

> Internal dashboard to auto-display our own TikTok follower count, views, and engagement daily. Read-only, no posting, no user data collected.

That's 119 characters.

---
## [00:56] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Try this:

> Internal dashboard to auto-display our TikTok follower count, views, and engagement daily. Read-only, no user data collected.

That's 124 characters — trim slightly:

> Internal tool to auto-display our TikTok follower count, views and engagement daily. Read-only, no user data collected.

That's 119 characters.

---
## [00:58] Session 29d34382

**Task:** I'm trying to do this properly:

**Summary:**
Now I have the full picture. A few high-impact improvements I'd suggest:

**1. Show which channels have live API data vs manual**
The scoreboard cards don't currently indicate whether the numbers came from the Meta API or were typed in. A small "API" or "Manual" badge on each KPI card would build trust in the data.

**2. Add a sparkline / 7-day trend to each KPI card**
Right now there's a delta % pill but no visual. A tiny 7-point line chart under each number (views, followers) would make trends

---
## [01:03] Session 29d34382

**Task:** I'm trying to do this properly:

**Files changed:**
- `src/app/(app)/dashboard/components/tabs/MarketingStudioTab.tsx`

**Summary:**
Pushed. Vercel is building now — should be live in ~2 minutes. Here's what changed:

- **API badge** — KPI cards show a green "API" chip when today's data came from Meta
- **Sparklines** — Views and Followers cards show a 7-day trend line
- **Smart manual log** — hides Instagram/Facebook inputs since they're auto-filled; shows "All channels auto-filled today ✓" when nothing needs manual entry
- **Pod activity** — each agent card now shows last run time and summary snippet (e.g. "Ran · 2h ago") i

---
## [10:42] Session 356e0739

**Task:** <ide_opened_file>The user opened the file /temp/readonly/command (0tc3z4) in the IDE. This may or may not be related to the current task.</ide_opened_file> there are sections in the marketing studio that overlapping off the edge

**Files changed:**
- `src/app/(app)/dashboard/components/tabs/MarketingStudioTab.tsx` *(re-edited)*

**Summary:**
Three targeted changes:

1. **Removed `overflow-visible` from the sparkline SVG** — the polyline points are already calculated to fit within the SVG bounds, so the stroke no longer bleeds outside the card.

2. **Added `overflow-hidden` + `min-w-0 shrink` to the KPI card bottom row** — clips any remaining overflow from the pill/sparkline area and lets the pill text compress if the card is narrow.

3. **Changed right column from `minmax(320px,0.7fr)` → `minmax(0,0.7fr)`** — the hard 320px minimum 

> **Re-edit detected** on `src/app/(app)/dashboard/components/tabs/MarketingStudioTab.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
