# Dev Log 2026-05-21

## [09:18] Session ae210eb4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/056_efficiency_architect.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Bud documented repair plan for bud (autonomy level 2 — branch creation requires level 3+). veri

**Files changed:**
- `.env.local`
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/lib/agents/agents/bud.ts`
- `src/lib/bud/orchestrator.ts`

**Summary:**
Pushed. The loop is broken at two levels:

- **`bud.ts`** — Bud will never add itself to the investigation list, so it won't trigger a repair branch for its own failures
- **`orchestrator.ts`** — even if something else calls `triggerInvestigation('bud', ...)`, the dedup check will return the existing open task instead of creating issue #9, #10, #11...

The existing open branches (#6, #7, #8) can be deleted — they're all empty and the underlying fixes are already on `main`.

---
## [09:23] Session ae210eb4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/056_efficiency_architect.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Bud documented repair plan for bud (autonomy level 2 — branch creation requires level 3+). veri

**Files changed:**
- `.env.local`
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/lib/agents/agents/bud.ts`
- `src/lib/bud/orchestrator.ts`

**Summary:**
Pushed. Once deployed:

- **Thought Stream** (the panel on the overview): hover any line → copy icon appears on the right. "Copy all" button appears next to "live" in the header.
- **Live Activity** (Activity tab): same — per-line copy on hover, "Copy all" in the card header.

Both copy in `[state · time] narrative` format, one line per entry — paste straight into a chat or bug report.

---
## [09:26] Session 09547fd2

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> I'm getting these issues: Structured failures No more 'unexpected error - no structured failure reason captured'. UNCLASSIFIED Admin 

**Files changed:**
- `src/app/(app)/dashboard/mission-control/page.tsx`
- `src/lib/agents/runtime.ts`
- `src/lib/bud/structured-failure.ts`

**Summary:**
No type errors. Here's what was wrong and what changed:

**Root cause:** When an agent run throws an exception, the runtime saved the error text in the `error` column but left `summary` as `NULL`. `buildStructuredFailure` classifies by reading `run.summary`, so it always got an empty string and fell through to `UNCLASSIFIED` (confidence 40%, step `agent_execution`).

**Three changes:**

1. [runtime.ts:494](src/lib/agents/runtime.ts#L494) — Added `summary: msg` to the failure `update()` call, so 

---
## [09:35] Session ae210eb4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/056_efficiency_architect.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Bud documented repair plan for bud (autonomy level 2 — branch creation requires level 3+). veri

**Files changed:**
- `.env.local`
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/lib/agents/agents/bud.ts`
- `src/lib/bud/orchestrator.ts`
- `src/lib/bud/os-view-model.ts`

**Summary:**
Pushed. Three things fixed:

**No more "repair branch for bud" spam** — the stale bud approval items are filtered out of the queue entirely so they can never be accidentally approved again.

**Investigate/Fix with Bud now skips the approval step** — at level 3 ("Branch & PR"), Bud creates the GitHub issue immediately when you click Investigate. The approval was backwards: level 2 had no approval gate, but level 3 was *more* restrictive because of the confidence check. Fixed so level 2+ auto-exec

---
## [09:57] Session 4786b547

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> The approval doesn't come up anymore and I'm getting these issues again but they don't fix themselves when I click fix with bud why i

**Files changed:**
- `src/app/api/bud/command/route.ts`
- `src/lib/bud/os-view-model.ts`

**Summary:**
Both changes are in place and TypeScript is clean. Here's what was wrong and what's fixed:

**Root cause**: When you click "Fix with Bud" on a broken agent, it goes through `/api/bud/command` which always sets `source_agent = 'bud'` on the task. The recent loop-fix commit added `if (task?.source_agent === 'bud') continue` to hide Bud self-investigation items — but that filter caught ALL bud-sourced approvals, including your legitimate ones. The difference is `requested_by`: self-investigation se

---
## [10:01] Session 4786b547

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> The approval doesn't come up anymore and I'm getting these issues again but they don't fix themselves when I click fix with bud why i

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/app/api/bud/command/route.ts`
- `src/lib/bud/os-view-model.ts`

**Summary:**
Still clean. Three things fixed in total:

1. **Filter bug** (`os-view-model.ts`) — approvals from your "Fix with Bud" clicks now pass through (they have your user ID as `requested_by`, not `'bud'`).

2. **Queue not updating** (`MissionControlClient.tsx`) — added a `useEffect` that syncs the `queue` state whenever the server sends fresh props after `router.refresh()`. The realtime subscription was already calling `refresh()` on new approval inserts — it just wasn't landing in state.

3. **Dedup*

---
## [10:02] Session 4786b547

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> The approval doesn't come up anymore and I'm getting these issues again but they don't fix themselves when I click fix with bud why i

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/app/api/bud/command/route.ts`
- `src/lib/bud/os-view-model.ts`

**Summary:**
Pushed. Vercel will pick that up and deploy. Once it's live:

- Refresh Mission Control and you'll see the 5 queued approvals from yesterday's clicks (all `approve_repair_plan` for the broken agents).
- Approve the one you want actioned, reject the duplicates.
- Going forward, clicking "Fix with Bud" will show the approval immediately — no manual refresh needed.

---
## [10:08] Session 4786b547

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> The approval doesn't come up anymore and I'm getting these issues again but they don't fix themselves when I click fix with bud why i

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/app/api/bud/command/route.ts`
- `src/lib/bud/os-view-model.ts`

**Summary:**
You have two Vercel projects both connected to the same GitHub repo (`main` branch), so every push triggers two builds:

- **`budsatworkaus`** — the real one: serves `budsatwork.com` and `www.budsatwork.com`
- **`budsatwork`** — a duplicate: only has Vercel preview URLs, no custom domain

The `budsatwork` project is redundant and just wastes build minutes. I can't delete Vercel projects via the API, but it's two clicks in the dashboard:

1. Go to [vercel.com/budsatwork0-ais-projects/budsatwork/s

---
## [10:20] Session d648bfeb

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> I'm still getting this issue: [repairing · 1m ago] Bud created repair branch `bud/fix-bud-1779322138920` and opened issue #14 for bud

**Files changed:**
- `src/lib/bud/orchestrator.ts`

**Summary:**
Clean. Here's what the fix does and why it stops the loop:

**Root cause**: `executeRepairPlan` had no dedup guard. Every approval call (one per queue item the user clicked "Fix with Bud" + approved) created a fresh GitHub branch regardless of whether a branch already existed for the same repair cycle. With 5 failing agents in the queue, that was 5 branches.

**The fix** — in `executeRepairPlan`, before touching GitHub:
1. Checks `bud_change_requests` for any open record created in the last 24h


---
## [10:26] Session d648bfeb

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> I'm still getting this issue: [repairing · 1m ago] Bud created repair branch `bud/fix-bud-1779322138920` and opened issue #14 for bud

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/lib/bud/orchestrator.ts`

**Summary:**
Clean. Here's what changed and why:

**Root cause**: `agent_health` items (Admin UX Designer, Lobby Theme Curator, Agent Architect) are derived from the agent's current health status — they're permanently "broken" in the DB until the underlying code is fixed. So after "Fix with Bud" or "Investigate" ran and called `router.refresh()`, the server sent the same broken agents back and the items snapped back to `DETECTED`.

**The fix**:
- Added `actionedIdsRef` — a `useRef<Set<string>>` that tracks e

---
## [10:27] Session d648bfeb

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> I'm still getting this issue: [repairing · 1m ago] Bud created repair branch `bud/fix-bud-1779322138920` and opened issue #14 for bud

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/lib/bud/orchestrator.ts`

**Summary:**
Pushed. Vercel will auto-deploy from that. Here's what's worth improving in Bud's OS efficiency:

---

**The biggest wins, roughly in order of impact:**

1. **Skip investigations for persistently broken agents** — Admin UX Designer, Lobby Theme Curator, Agent Architect have been broken for 5+ days. Every Bud cycle detects them, creates tasks, and wastes LLM budget. They need to either be disabled or repaired. Bud should stop re-investigating the same agent more than once per 48h and instead emit

---
## [10:32] Session d648bfeb

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> I'm still getting this issue: [repairing · 1m ago] Bud created repair branch `bud/fix-bud-1779322138920` and opened issue #14 for bud

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/lib/agents/agents/bud.ts`
- `src/lib/bud/orchestrator.ts`

**Summary:**
All three commits are live and deploying. Here's what each cron cycle will now do differently:

| Before | After |
|---|---|
| Fetched ALL agent runs for 24h/7d | Only fetches runs for enabled agents |
| Called Claude on every 15-min tick | Skips Claude when agents/KPIs unchanged — likely 60-70% of quiet cycles |
| Re-investigated Admin UX Designer etc every cycle | Emits one escalation insight, stops re-investigating until actually fixed |
| `bud_insights` table grows forever | Rows >30 days de

---
## [10:34] Session 1e80f7f8

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> how can we improve or optimise the buds os

**Summary:**
Here's a clear picture of where the system is today and where the highest-value improvements are:

---

## Current state

The OS is solid: circuit breaker, zombie reaper, concurrency guard, structured failures, exponential backoff, skip-on-no-change, and Efficiency Architect are all in place. The main bottlenecks now are **cost**, **capability ceiling**, and **signal quality**.

---

## High-impact improvements

### 1. Prompt caching — biggest cost win available

[runtime.ts:88-101](src/lib/agen

---
## [10:44] Session 1e80f7f8

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> how can we improve or optimise the buds os

**Files changed:**
- `src/app/api/agents/cron/fleet/route.ts`
- `src/lib/agents/agents/bud.ts`
- `src/lib/agents/agents/competitor-watcher.ts`
- `src/lib/agents/agents/crew-briefing.ts`
- `src/lib/agents/agents/internal-qa.ts`
- `src/lib/agents/agents/lapsed-win-back.ts`
- `src/lib/agents/agents/photo-qa.ts`
- `src/lib/agents/agents/reviews.ts`
- `src/lib/agents/agents/scheduling.ts`
- `src/lib/agents/agents/whs-safety-reminder.ts`
- `src/lib/agents/runtime.ts`
- `src/lib/agents/types.ts`

**Summary:**
All seven improvements are implemented and committed. Here's a summary of what landed:

---

## What was implemented

| # | Improvement | Where |
|---|---|---|
| 1 | **Prompt caching** | `runtime.ts` — system prompts sent with `cache_control: ephemeral`, caching beta header, cache token counts tracked | 
| 2 | **Per-agent model routing** | `types.ts` adds `preferredModel`; 8 lightweight agents switched to `haiku` (~5× cheaper on input) |
| 3 | **Bud writes code** | `repair-executor.ts` now calls

---
## [10:44] Session 1e80f7f8

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/.env.local in the IDE. This may or may not be related to the current task.</ide_opened_file> how can we improve or optimise the buds os

**Files changed:**
- `src/app/api/agents/cron/fleet/route.ts`
- `src/lib/agents/agents/bud.ts`
- `src/lib/agents/agents/competitor-watcher.ts`
- `src/lib/agents/agents/crew-briefing.ts`
- `src/lib/agents/agents/internal-qa.ts`
- `src/lib/agents/agents/lapsed-win-back.ts`
- `src/lib/agents/agents/photo-qa.ts`
- `src/lib/agents/agents/reviews.ts`
- `src/lib/agents/agents/scheduling.ts`
- `src/lib/agents/agents/whs-safety-reminder.ts`
- `src/lib/agents/runtime.ts`
- `src/lib/agents/types.ts`

**Summary:**
Pushed. Vercel will auto-deploy from that — commit `e41bf55` (Seven Buds OS efficiency improvements). Once it's live, run migration `057` in Supabase to activate the quality scores and pgvector columns.

---
## [11:40] Session 05a5861a

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> how can we genuinely fix the broken agents

**Files changed:**
- `src/lib/agents/agents/admin-ux-designer.ts`
- `src/lib/agents/agents/agent-architect.ts`
- `src/lib/agents/agents/lobby-theme-curator.ts`
- `src/lib/agents/runtime.ts`

**Summary:**
Clean. Here's what was actually broken and what was fixed:

**Root cause**: When Bud OS launched (May 19), `runtime.ts` was updated to validate every agent's `result.output` against a strict Zod schema (`AgentOutputSchema`) requiring `status`, `confidence`, `risk_level`, `findings: string[]`, and `recommended_actions`. The three agents predate this and returned lightweight output objects — so every run failed the check and got marked `needs_repair`, generating Bud tasks.

**Fixes applied**:

1. 

---
## [11:46] Session 05a5861a

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> how can we genuinely fix the broken agents

**Files changed:**
- `src/lib/agents/agents/admin-ux-designer.ts`
- `src/lib/agents/agents/agent-architect.ts`
- `src/lib/agents/agents/bud.ts`
- `src/lib/agents/agents/lobby-theme-curator.ts`
- `src/lib/agents/runtime.ts`
- `src/lib/bud/orchestrator.ts`
- `src/lib/bud/repair-executor.ts`

**Summary:**
All clean. Here's what was done and why it matters:

---

**Three genuine improvements to Bud's repair capability:**

**1. `affectedFiles` now populated (orchestrator.ts)**
The repair executor has always read `structured_failure.affectedFiles` to know which files to patch — but `buildFailureReport` never populated that field. It was always empty, so the LLM got `"No affected files could be read"` on every repair attempt. Now the agent's source file (`src/lib/agents/agents/${agentId}.ts`) is alwa

---
## [11:56] Session 05a5861a

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> how can we genuinely fix the broken agents

**Files changed:**
- `src/lib/agents/agents/admin-ux-designer.ts`
- `src/lib/agents/agents/agent-architect.ts`
- `src/lib/agents/agents/bud.ts`
- `src/lib/agents/agents/lobby-theme-curator.ts`
- `src/lib/agents/runtime.ts`
- `src/lib/agents/types.ts`
- `src/lib/bud/health.ts`
- `src/lib/bud/orchestrator.ts`
- `src/lib/bud/repair-executor.ts`

**Summary:**
All clean. Here's a precise summary of what changed and why each one matters:

---

**1. `quality_score` in health scoring** (`health.ts`)

The 057 migration added a DB trigger that scores runs 0–1 based on whether users approve or reject proposed actions. That data was never read. `scoreAgentHealth` now checks the average quality score across recently-scored runs: below 0.45 costs 15 points; below 0.35 costs 30. An agent that keeps proposing actions users reject will drift into `watch` or `need

---
## [11:57] Session 05a5861a

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> how can we genuinely fix the broken agents

**Files changed:**
- `src/lib/agents/agents/admin-ux-designer.ts`
- `src/lib/agents/agents/agent-architect.ts`
- `src/lib/agents/agents/bud.ts`
- `src/lib/agents/agents/lobby-theme-curator.ts`
- `src/lib/agents/runtime.ts`
- `src/lib/agents/types.ts`
- `src/lib/bud/health.ts`
- `src/lib/bud/orchestrator.ts`
- `src/lib/bud/repair-executor.ts`

**Summary:**
Pushed. Vercel will pick up the commit and deploy automatically. The commit covers all 8 changes across both sessions — the three broken agent fixes, the runtime normalisation, and all five Bud OS detection/repair improvements.

---
## [12:04] Session 05a5861a

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> how can we genuinely fix the broken agents

**Files changed:**
- `src/lib/agents/agents/admin-ux-designer.ts`
- `src/lib/agents/agents/agent-architect.ts`
- `src/lib/agents/agents/bud.ts`
- `src/lib/agents/agents/lobby-theme-curator.ts`
- `src/lib/agents/runtime.ts`
- `src/lib/agents/types.ts`
- `src/lib/bud/health.ts`
- `src/lib/bud/orchestrator.ts`
- `src/lib/bud/repair-executor.ts`

**Summary:**
Pushed. Three things were wrong with that message:

**"Bud skipped duplicate branch for bud"** — Bud was attempting to repair itself. This should never happen. `triggerInvestigation` and `executeRepairPlan` now both exit early when the target is `bud`, with a clear activity event telling you to review it manually.

**"existing branch `bud/fix-bud-...` is already open"** — the dedup query matched *any* open `bud_change_requests` row from the past 24h, not just one for the same agent. If you had a

---
## [12:10] Session 58233386

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> I genuinely want buds os to be able to fix agents but I'm getting this: [blocked · just

**Files changed:**
- `src/lib/bud/orchestrator.ts`

**Summary:**
Clean. Both guards are removed and the dedup logic remains intact — so if Bud fails repeatedly, it'll still only create one branch per 24h window rather than spamming.

Two changes made to [orchestrator.ts](src/lib/bud/orchestrator.ts):

1. **`triggerInvestigation`** — removed the block that prevented Bud from investigating itself (lines 246–255 old). Bud can now produce a failure report for itself, just like any other agent.
2. **`approveRepair`** — removed the block that failed tasks targeting

---
## [12:12] Session 58233386

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> I genuinely want buds os to be able to fix agents but I'm getting this: [blocked · just

**Files changed:**
- `src/lib/bud/orchestrator.ts`

**Summary:**
Nothing is stopping us from building this — the pieces are almost all there. Here's what the full loop would look like:

**Current flow:**
Failure detected → Bud creates task → Branch + GitHub issue created → **you implement manually**

**What you're describing:**
Failure detected → Bud writes repair plan → **Claude API generates the actual code diff** → code committed to branch → draft PR opened → you review what was written → approve → Vercel deploys

**What needs building:**

1. **`src/lib/bu

---
## [12:19] Session 58233386

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> I genuinely want buds os to be able to fix agents but I'm getting this: [blocked · just

**Files changed:**
- `src/lib/bud/orchestrator.ts`

**Summary:**
Nothing is fundamentally blocking any of this. It's a full autonomous repair operating system and every layer is achievable with what's already in the stack. Here's how I'd architect it:

---

## Architecture: Bud Autonomous Repair Pipeline

### Phase 1 — Intelligence Layer
**Root Cause Analysis + Historical Context**
- Extend `buildFailureReport()` with confidence scoring (weighted: error type, frequency, recency, blast radius)
- Similarity search across: `agent_runs` (Supabase), GitHub Issues 

> **ADR signal detected.** Consider running `npx tsx scripts/vault-adr.ts` to record this decision formally.

---
## [12:24] Session 58233386

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> I genuinely want buds os to be able to fix agents but I'm getting this: [blocked · just

**Files changed:**
- `src/lib/bud/github-executor.ts`
- `src/lib/bud/orchestrator.ts`
- `src/lib/bud/repair-executor.ts`

**Summary:**
Phase 1 is done. Here's what changed across 2 files:

---

**[github-executor.ts](src/lib/bud/github-executor.ts)** — `searchIssues()` added. Queries GitHub for past `[Bud]` issues on the failing agent. Returns empty array silently if GitHub isn't configured.

**[repair-executor.ts](src/lib/bud/repair-executor.ts)** — 4 changes:

1. **`searchHistoricalContext()`** — new function that in parallel pulls: past repairs from `bud_repair_learnings` by root cause type, recent agent failures from `agent

---
## [12:32] Session a608df58

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/057_agent_quality_and_embeddings.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> do phase 2 — Validation pipeline	~1 day	GitHub Actions / subprocess wiring

**Files changed:**
- `src/lib/bud/github-executor.ts`
- `src/lib/bud/repair-executor.ts`
- `supabase/migrations/058_bud_validation_pipeline.sql`

**Summary:**
Zero type errors. Phase 2 is done. Here's what was built:

---

### Phase 2 — Validation Pipeline

**[src/lib/bud/github-executor.ts](src/lib/bud/github-executor.ts)** — 4 additions:

| Export | What it does |
|---|---|
| `deleteBranch(name)` | Deletes a repair branch for rollback; silently handles 422 (already gone) |
| `getLatestWorkflowRun(branch, afterTimestamp?)` | Fetches the most recent `push`-event workflow run on a branch, filtered to runs that started after the patch was pushed |
| `po

---
## [12:42] Session 456d2624

**Task:** <ide_selection>The user selected the lines 1 to 22 from /Users/jacksontaylor/budsatwork/supabase/migrations/058_bud_validation_pipeline.sql: -- 058: Bud validation pipeline — CI tracking columns on bud_repair_executions -- -- Adds four columns to track the GitHub Actions CI result for each autonomou

**Files changed:**
- `src/lib/bud/design-constitution.ts`
- `src/lib/bud/repair-executor.ts`
- `src/lib/bud/visual-scorer.ts`
- `supabase/migrations/059_bud_taste.sql`

**Summary:**
Phase 3 is done. Here's what was built:

---

**[src/lib/bud/design-constitution.ts](src/lib/bud/design-constitution.ts)** — The Design Constitution: 20 rules across 7 categories (colour, glass morphism, typography, spacing, components, accessibility, anti-patterns), each with a severity (`required` / `recommended` / `nice-to-have`), a description, and a `check` field telling Claude exactly what to look for in code. Exports `constitutionToPrompt()` for the Vision prompt, `isUiFile()` to identify

---
## [13:14] Session 525d2477

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/059_bud_taste.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> do phase 4 — Browser agent	~2 days	Playwright flows for all golden paths

**Files changed:**
- `src/lib/agents/agents/browser-agent.ts`
- `src/lib/agents/registry.ts`
- `src/lib/bud/browser-executor.ts`
- `src/lib/bud/repair-executor.ts`
- `supabase/migrations/060_bud_browser_agent.sql`
- `tests/e2e/golden-paths/auth-redirect.spec.ts`
- `tests/e2e/golden-paths/homepage.spec.ts`
- `tests/e2e/golden-paths/quote-cleaning.spec.ts`
- `tests/e2e/golden-paths/quote-commercial.spec.ts`
- `tests/e2e/golden-paths/quote-ndis.spec.ts`

**Summary:**
Phase 4 is done. Zero TypeScript errors. Here's what was built:

---

## Phase 4 — Browser Agent

### Golden-path test specs (`tests/e2e/golden-paths/`)

| File | Coverage |
|---|---|
| [homepage.spec.ts](tests/e2e/golden-paths/homepage.spec.ts) | Title, nav, hero CTA, services section, broken images, mobile viewport |
| [quote-cleaning.spec.ts](tests/e2e/golden-paths/quote-cleaning.spec.ts) | Home context → Cleaning → step 2 scope selection → price estimate → step 3 contact form; Window Cleanin

---
## [13:22] Session 09661c19

**Task:** <ide_selection>The user selected the lines 1 to 68 from /Users/jacksontaylor/budsatwork/supabase/migrations/060_bud_browser_agent.sql: -- 060: Bud Browser Agent — golden-path Playwright test run storage -- -- Stores results from every Playwright browser test run triggered by Bud OS. -- Runs are asso

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`
- `src/app/(app)/dashboard/mission-control/page.tsx`
- `src/lib/bud/os-view-model.ts`
- `src/lib/bud/repair-executor.ts`
- `supabase/migrations/061_bud_pr_url.sql`

**Summary:**
Zero type errors. Phase 5 is done. Here's what was built:

---

**Migration [061_bud_pr_url.sql](supabase/migrations/061_bud_pr_url.sql)**
`pr_url` and `issue_url` columns added to `bud_repair_executions`. Previously `prUrl`/`issueUrl` were returned from `executeRepairPipeline` but never written to the DB, so Mission Control had no way to surface them.

**[repair-executor.ts](src/lib/bud/repair-executor.ts)**
Final `updateExecution` call now includes `pr_url` and `issue_url` so they persist on t

---
