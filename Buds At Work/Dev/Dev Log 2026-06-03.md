# Dev Log 2026-06-03

## [00:35] Session 87072ced

**Task:** <ide_selection>The user selected the lines 1 to 70 from /Users/jacksontaylor/budsatwork/supabase/migrations/080_bud_approval_cleanup.sql: -- Bud OS approval cleanup support. -- Adds archival/blocking states and a truth view so Mission Control can separate -- live decisions from historical unsafe app

**Summary:**
Everything is clean. Here's the full Phase 4 report:

---

**Dry-run count: 0 (not 68) — explained**

The 68 `blocked_historical` rows are already `status='blocked'` in the database. The dry-run count measures *new* candidates to block (pending rows that classify as Blocked) — that work was done by a prior run. The `blocked_historical: 68` in the "Before" section confirms the cleanup is complete, not pending.

---

**--apply result (no-op confirmed)**

| Metric | Before | After |
|---|---|---|
|

---
## [00:47] Session 87072ced

**Task:** <ide_selection>The user selected the lines 1 to 70 from /Users/jacksontaylor/budsatwork/supabase/migrations/080_bud_approval_cleanup.sql: -- Bud OS approval cleanup support. -- Adds archival/blocking states and a truth view so Mission Control can separate -- live decisions from historical unsafe app

**Summary:**
I have everything needed. Here is the full Phase 5A audit.

---

## Phase 5A — Customer Reply Root Cause Audit

### What actually happened (two distinct phases)

The agent has run **248 times** since inception. It has **never once drafted a reply or proposed an action.** There are two separate failure modes operating back-to-back.

---

### Phase 1 — Hard failure: Schema drift (RESOLVED)

**Window:** 2026-05-28 10:00 UTC → 2026-05-30 05:30 UTC (~43.5 hours)
**Runs:** 87 failed, status `failed`
*

---
## [00:51] Session 87072ced

**Task:** <ide_selection>The user selected the lines 1 to 70 from /Users/jacksontaylor/budsatwork/supabase/migrations/080_bud_approval_cleanup.sql: -- Bud OS approval cleanup support. -- Adds archival/blocking states and a truth view so Mission Control can separate -- live decisions from historical unsafe app

**Summary:**
I have all the data needed. Here is the full Phase 5B audit.

---

## Phase 5B — Lead Communication Architecture Audit

---

### Lead source matrix

| Source | Email | Phone | Social thread ID | Reply channel wired? | Intake route |
|---|---|---|---|---|---|
| **Website quote form** | ✅ always | ✅ usually | — | ✅ email via Resend | `/api/quotes` |
| **Messenger** | ❌ never | ❌ never | ✅ PSID in conversation `metadata` JSONB | ❌ none | `/api/webhooks/messenger` → `/api/leads/messenger` |
| **Inst

> **ADR signal detected.** Consider running `npx tsx scripts/vault-adr.ts` to record this decision formally.

---
## [08:31] Session 87072ced

**Task:** <ide_selection>The user selected the lines 1 to 70 from /Users/jacksontaylor/budsatwork/supabase/migrations/080_bud_approval_cleanup.sql: -- Bud OS approval cleanup support. -- Adds archival/blocking states and a truth view so Mission Control can separate -- live decisions from historical unsafe app

**Files changed:**
- `src/app/api/webhooks/messenger/route.ts` *(re-edited)*
- `tests/api/instagram-webhook.test.ts`

**Summary:**
26 tests pass, zero type errors. Here is the full output.

---

### Files changed

**`src/app/api/webhooks/messenger/route.ts`** — 4 targeted edits, no new imports

**`tests/api/instagram-webhook.test.ts`** — new file, 18 tests

---

### What changed in the route

| Location | Before | After |
|---|---|---|
| Graph fields constants | Single `GRAPH_FIELDS = 'first_name,last_name'` | `MESSENGER_GRAPH_FIELDS` and `INSTAGRAM_GRAPH_FIELDS = 'name'` (Instagram Graph API only exposes `name`) |
| `Graph

> **Re-edit detected** on `src/app/api/webhooks/messenger/route.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [08:40] Session 87072ced

**Task:** <ide_selection>The user selected the lines 1 to 70 from /Users/jacksontaylor/budsatwork/supabase/migrations/080_bud_approval_cleanup.sql: -- Bud OS approval cleanup support. -- Adds archival/blocking states and a truth view so Mission Control can separate -- live decisions from historical unsafe app

**Files changed:**
- `src/app/api/leads/messenger/route.ts` *(re-edited)*
- `src/app/api/webhooks/messenger/route.ts` *(re-edited)*
- `src/lib/leads/reply-channel.ts`
- `supabase/migrations/084_lead_reply_channel.sql`
- `tests/api/instagram-webhook.test.ts` *(re-edited)*
- `tests/api/messenger-ingest.test.ts`
- `tests/lib/lead-reply-channel.test.ts`

**Summary:**
52 tests, clean build, migration applied and verified. Here's the full output.

---

### Migration applied — `084_lead_reply_channel`

**Backfill result (live production data):**

| Table | Rows updated | Result |
|---|---|---|
| `leads` (4 rows, all source='messenger') | 4 | `reply_channel='messenger'`, `messenger_psid='31091024067211498'` |
| `lead_conversations` (4 rows) | 4 | `external_sender_id='31091024067211498'`, `metadata.sender_psid` preserved unchanged |
| `leads.instagram_user_id` | 

> **Re-edit detected** on `src/app/api/leads/messenger/route.ts`, `src/app/api/webhooks/messenger/route.ts`, `tests/api/instagram-webhook.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [08:52] Session 87072ced

**Task:** <ide_selection>The user selected the lines 1 to 70 from /Users/jacksontaylor/budsatwork/supabase/migrations/080_bud_approval_cleanup.sql: -- Bud OS approval cleanup support. -- Adds archival/blocking states and a truth view so Mission Control can separate -- live decisions from historical unsafe app

**Files changed:**
- `src/app/api/leads/messenger/route.ts` *(re-edited)*
- `src/app/api/webhooks/messenger/route.ts` *(re-edited)*
- `src/lib/agents/agents/customer-reply.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `src/lib/leads/reply-channel.ts`
- `supabase/migrations/084_lead_reply_channel.sql`
- `tests/api/instagram-webhook.test.ts` *(re-edited)*
- `tests/api/messenger-ingest.test.ts`
- `tests/lib/customer-reply-agent.test.ts` *(re-edited)*
- `tests/lib/lead-reply-channel.test.ts`
- `tests/lib/send-messenger-dispatch.test.ts` *(re-edited)*

**Summary:**
Clean. Here's the Phase 5E summary.

---

### Files changed

| File | Type | What changed |
|---|---|---|
| `src/lib/agents/agents/customer-reply.ts` | Rewritten | Branches on `reply_channel`; proposes `send_messenger`, `send_email`, or `flag_for_review` per lead |
| `src/lib/agents/runtime.ts` | Modified | `send_messenger` added to dispatch table; `sendMessengerEffect()` added after `sendEmailEffect` |
| `tests/lib/customer-reply-agent.test.ts` | New | 12 tests covering all channel paths and id

> **Re-edit detected** on `src/app/api/leads/messenger/route.ts`, `src/app/api/webhooks/messenger/route.ts`, `src/lib/agents/runtime.ts`, `tests/api/instagram-webhook.test.ts`, `tests/lib/customer-reply-agent.test.ts`, `tests/lib/send-messenger-dispatch.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:15] Session 87072ced

**Task:** <ide_selection>The user selected the lines 1 to 70 from /Users/jacksontaylor/budsatwork/supabase/migrations/080_bud_approval_cleanup.sql: -- Bud OS approval cleanup support. -- Adds archival/blocking states and a truth view so Mission Control can separate -- live decisions from historical unsafe app

**Files changed:**
- `src/app/api/leads/messenger/route.ts` *(re-edited)*
- `src/app/api/webhooks/messenger/route.ts` *(re-edited)*
- `src/lib/agents/agents/bud-observer.ts` *(re-edited)*
- `src/lib/agents/agents/customer-reply.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `src/lib/bud/health.ts` *(re-edited)*
- `src/lib/leads/reply-channel.ts`
- `supabase/migrations/084_lead_reply_channel.sql`
- `tests/api/instagram-webhook.test.ts` *(re-edited)*
- `tests/api/messenger-ingest.test.ts`
- `tests/lib/customer-reply-agent.test.ts` *(re-edited)*
- `tests/lib/lead-reply-channel.test.ts`

**Summary:**
Phase 5F summary.

---

### Files changed

| File | Type | What changed |
|---|---|---|
| `src/lib/bud/health.ts` | Modified | Added 2 noise phrases; exported `isStalledAgent`; integrated stalled check into `scoreAgentHealth` |
| `src/lib/agents/agents/bud-observer.ts` | Modified | Added import of `isStalledAgent`; added stalled-agent data collection (query 7); added stalled agents to LLM snapshot; added post-LLM deterministic trigger loop |
| `tests/lib/stalled-agent.test.ts` | New | 18 tests a

> **Re-edit detected** on `src/app/api/leads/messenger/route.ts`, `src/app/api/webhooks/messenger/route.ts`, `src/lib/agents/agents/bud-observer.ts`, `src/lib/agents/runtime.ts`, `src/lib/bud/health.ts`, `tests/api/instagram-webhook.test.ts`, `tests/lib/customer-reply-agent.test.ts`, `tests/lib/send-messenger-dispatch.test.ts`, `tests/lib/stalled-agent.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:23] Session 29424d95

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/077_marketing_studio.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 6A only. Goal: Make every Mission Control improvement approval prove why it exists 

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/lib/bud/os-view-model.ts` *(re-edited)*
- `tests/lib/bud-os-view-model.test.ts` *(re-edited)*

**Summary:**
Phase 6A is done. Here's the full delivery report:

---

## Phase 6A — Improvement approval evidence

### Files changed

**[src/lib/bud/os-view-model.ts](src/lib/bud/os-view-model.ts)**
- Added `affected_area: string | null` and `signal_type: string | null` to `BudOsApprovalDetail` type
- `buildApprovalDetailFromBudApproval()` — populates both from `payload.affected_area` and `payload.signal_type` (already extracted at the `signalArea` variable)
- `buildApprovalDetailFromAgentAction()` — both se

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/lib/bud/os-view-model.ts`, `tests/lib/bud-os-view-model.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:28] Session 29424d95

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/077_marketing_studio.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 6A only. Goal: Make every Mission Control improvement approval prove why it exists 

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/lib/bud/os-view-model.ts` *(re-edited)*
- `tests/lib/bud-os-view-model.test.ts` *(re-edited)*

**Summary:**
---

## Baseline stabilisation report

### Failure 1 — `ux-evolution-engine.test.ts`

**Test name:** `normalizes UX, design, and agent evolution signals into Bud recommendations`
**File:** [tests/lib/ux-evolution-engine.test.ts](tests/lib/ux-evolution-engine.test.ts)
**Root cause:** API signature mismatch. The test passes `adminUxProposals`, `designInsights`, and `agentEvolutions` to `buildUxEvolutionRecommendations()`, but the function was refactored in commit `00d986e` (Phase 2 Mission Control

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/lib/bud/os-view-model.ts`, `tests/lib/bud-os-view-model.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:35] Session 29424d95

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/077_marketing_studio.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 6A only. Goal: Make every Mission Control improvement approval prove why it exists 

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/lib/bud/os-view-model.ts` *(re-edited)*
- `tests/lib/bud-os-view-model.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts`

**Summary:**
Build clean. Phase 6B is done.

---

## Phase 6B delivery report

### Files changed

**[src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx](src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx)**
— JSX render order only. No props changed, no data fetching changed, no component logic changed.

**[tests/lib/mission-control-layout.test.ts](tests/lib/mission-control-layout.test.ts)** *(new)*
— 5 structural tests that read the component source and assert section orde

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/lib/bud/os-view-model.ts`, `tests/lib/bud-os-view-model.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [09:49] Session 29424d95

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/077_marketing_studio.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 6A only. Goal: Make every Mission Control improvement approval prove why it exists 

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/lib/bud/os-view-model.ts` *(re-edited)*
- `tests/lib/bud-os-view-model.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
Build clean. Phase 7 is done.

---

## Phase 7 delivery report

### Files changed

**[src/lib/bud/os-view-model.ts](src/lib/bud/os-view-model.ts)**
Added and exported (pure TS, no React — fully testable):
- `HIGH_VALUE_AGENT_IDS` — Set of the 6 required high-value agent IDs
- `MEDIUM_VALUE_AGENT_IDS` — Set of 12 operational support agents
- `AgentBizValue` type — `'high' | 'medium' | 'low'`
- `AgentStatusDerived` type — `'healthy' | 'watch' | 'failing' | 'disabled'`
- `deriveAgentBizValue(id)` —

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/lib/bud/os-view-model.ts`, `tests/lib/bud-os-view-model.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:00] Session 29424d95

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/077_marketing_studio.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 6A only. Goal: Make every Mission Control improvement approval prove why it exists 

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx` *(re-edited)*
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/(app)/dashboard/mission-control/page.tsx` *(re-edited)*
- `src/lib/bud/os-view-model.ts` *(re-edited)*
- `tests/lib/bud-os-view-model.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
Build clean. Phase 7.5 done.

---

## Phase 7.5 delivery report

### Files changed

**[src/lib/bud/os-view-model.ts](src/lib/bud/os-view-model.ts)**
New exports:
- `AgentImpactEntry` type — `{ actions_last_30d, approvals_last_30d, outputs_last_30d }`
- `AgentImpactMap` type — `Record<agent_id, AgentImpactEntry>`
- `buildAgentImpactMap({ actionRows, outputRows })` — pure function, aggregates raw DB rows into per-agent counts; fully testable without a DB

**[src/app/(app)/dashboard/mission-control

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`, `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/app/(app)/dashboard/mission-control/page.tsx`, `src/lib/bud/os-view-model.ts`, `tests/lib/bud-os-view-model.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:07] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`
- `tests/lib/mission-control-layout.test.ts`

**Summary:**
Build passes. Here's the complete summary of what was done:

---

## Phase 8 — Ask Bud Quick Commands

### Files changed

1. [OverviewCore.tsx](src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx) — `StateAndAsk` function
2. [mission-control-layout.test.ts](tests/lib/mission-control-layout.test.ts) — new Phase 8 describe block

### What changed in `StateAndAsk`

| Before | After |
|---|---|
| `ask()` reads `draft` only | `ask(override?)` accepts chip text directly |
| `toast.err

---
## [10:11] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`
- `tests/lib/mission-control-layout.test.ts`

**Summary:**
I have everything I need. Here is the full audit.

---

## Ask Bud — Implementation Audit

---

### Request flow

```
Browser (OverviewCore.tsx)
  └── POST /api/bud/command  { command: string }
        │
        ├─ 1. Auth check — admin only (401 if not)
        ├─ 2. classifyBudCommand(command)
        │       → intent, risk_level, autonomy_level, needs_approval, action_type
        │
        ├─ 3. Parallel reads (system context for Haiku prompt)
        │       bud_tasks          — open task c

---
## [10:16] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
All green. Here's the summary:

---

## Phase 8A — Changes made

**[OverviewCore.tsx](src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx)**

| Change | Detail |
|---|---|
| Removed chip | `Find dead agents` |
| Renamed chip label | `Find stalled agents` → `Investigate stalled agents` |
| Updated `<label>` | `"Ask Bud — creates a real task you can approve below"` → `"Ask Bud"` |
| Added UI copy | `"Ask Bud creates a tracked task. Some commands may require approval before action.

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:31] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/bud/command/route.ts` *(re-edited)*
- `tests/api/bud-command.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
All green. Here's the Phase 8B summary:

---

## Phase 8B — Changes

### [route.ts](src/app/api/bud/command/route.ts)

| Before | After |
|---|---|
| `command = body.command?.trim()` | `command = normalizeCommand(raw)` — trim + lowercase + collapse spaces |
| System context reads first, dedup after | Dedup first, system context reads only if no duplicate |
| Dedup fires only when `existingTask && existingApproval` | Dedup fires when `existingTask` alone — covers investigate/review/audit |
| Haik

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/app/api/bud/command/route.ts`, `tests/api/bud-command.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:37] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/bud/command/route.ts` *(re-edited)*
- `tests/api/bud-command.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
I have everything. Here is the full verification report.

---

## Mission Control — Production Verification Pass

---

### Current counts

| Signal | Value |
|---|---|
| Enabled agents | 32 |
| Disabled agents | 8 |
| Planned agents | 7 |
| `bud_tasks` open (last 48h) | 0 pending — 2 completed, 9 archived |
| `bud_approval_queue` pending | 3 rows |
| `bud_approval_queue` blocked | 68 rows |
| `bud_approval_queue` approved | 45 rows |
| Messenger leads (unanswered) | 4 |
| Pending `flag_for_revie

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/app/api/bud/command/route.ts`, `tests/api/bud-command.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:47] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/bud/command/route.ts` *(re-edited)*
- `src/app/api/leads/messenger/route.ts`
- `src/app/api/webhooks/messenger/route.ts`
- `supabase/migrations/085_backfill_messenger_psid.sql`
- `tests/api/bud-command.test.ts` *(re-edited)*
- `tests/api/messenger-ingest.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
All green. Here is the complete Phase 8C summary.

---

## Phase 8C — Output

### Audit finding

The production verification report incorrectly concluded `messenger_psid` was null. All 4 messenger leads in production already have `messenger_psid = '31091024067211498'` and `reply_channel = 'messenger'`. The 4 pending `flag_for_review` actions are not linked to any messenger leads. Customer Reply will create `send_messenger` approvals on its next scheduled run without any data fix needed.

The **r

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/app/api/bud/command/route.ts`, `tests/api/bud-command.test.ts`, `tests/api/messenger-ingest.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:52] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/bud/command/route.ts` *(re-edited)*
- `src/app/api/leads/messenger/route.ts`
- `src/app/api/webhooks/messenger/route.ts`
- `supabase/migrations/085_backfill_messenger_psid.sql`
- `tests/api/bud-command.test.ts` *(re-edited)*
- `tests/api/messenger-ingest.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
I have everything needed. The verification is complete — no trigger needed. Here is the full report.

---

## Customer Reply Verification Report

---

### Pre-run state (verified via DB)

**Check 1 — Customer Reply sees the 4 unanswered Messenger leads**

All 4 leads confirmed in DB with `first_response_at = null`:

| Lead ID | Name | reply_channel | messenger_psid | first_response_at |
|---|---|---|---|---|
| 5d2d1851 | Jackson Taylor | messenger | 31091024067211498 | null |
| dfd8f9df | Jackso

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/app/api/bud/command/route.ts`, `tests/api/bud-command.test.ts`, `tests/api/messenger-ingest.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:54] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/bud/command/route.ts` *(re-edited)*
- `src/app/api/leads/messenger/route.ts`
- `src/app/api/webhooks/messenger/route.ts`
- `supabase/migrations/085_backfill_messenger_psid.sql`
- `tests/api/bud-command.test.ts` *(re-edited)*
- `tests/api/messenger-ingest.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
The diff makes the gap explicit. The **committed production code** inserts new Messenger leads without `messenger_psid`, `reply_channel`, or `external_sender_id` — all three were missing from the original `ingestEvent`. The new message just proved it live.

---

## Findings

### What the new message revealed

Lead `7ac3156e` was created moments ago by the real webhook. Its columns tell the full story:

| Column | Value | Expected |
|---|---|---|
| `messenger_psid` | `null` | `31091024067211498` 

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/app/api/bud/command/route.ts`, `tests/api/bud-command.test.ts`, `tests/api/messenger-ingest.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [10:58] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/bud/command/route.ts` *(re-edited)*
- `src/app/api/leads/messenger/route.ts`
- `src/app/api/webhooks/messenger/route.ts`
- `supabase/migrations/085_backfill_messenger_psid.sql`
- `tests/api/bud-command.test.ts` *(re-edited)*
- `tests/api/messenger-ingest.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
Build is live. The deployment picked up the push immediately.

**`dpl_3kghN4Tyec4z6RQLMsdn2j9ZkvZN`** — `BUILDING` → `production`  
Commit `ad4fb61` · [Inspect](https://vercel.com/budsatwork0-ais-projects/budsatworkaus/3kghN4Tyec4z6RQLMsdn2j9ZkvZN)

Previous production was `6f276c0` (runtime truth layer). This build replaces it.

**What lands when READY:**
- Ask Bud quick command chips (Phase 8A)
- Command dedup + normalisation — no duplicate pending tasks, no wasted Haiku calls (Phase 8B)
- Mes

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/app/api/bud/command/route.ts`, `tests/api/bud-command.test.ts`, `tests/api/messenger-ingest.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [11:02] Session 315dc6c4

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/084_lead_reply_channel.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Implement Phase 8 only. Goal: Turn Ask Bud into a useful command centre that creates real Bud tas

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/bud/command/route.ts` *(re-edited)*
- `src/app/api/leads/messenger/route.ts`
- `src/app/api/webhooks/messenger/route.ts`
- `supabase/migrations/085_backfill_messenger_psid.sql`
- `tests/api/bud-command.test.ts` *(re-edited)*
- `tests/api/messenger-ingest.test.ts` *(re-edited)*
- `tests/lib/mission-control-layout.test.ts` *(re-edited)*

**Summary:**
Yes. The fix is working.

Lead `5dd029e9` — created at `01:02` (after deployment, ~10 minutes after the push) — shows all three fields correctly set for the first time:

| Field | Before deploy | After deploy |
|---|---|---|
| `messenger_psid` | `null` | `31091024067211498` ✅ |
| `reply_channel` | `null` | `messenger` ✅ |
| `external_sender_id` | `null` | `31091024067211498` ✅ |

That was your second test message. The ingest path is fixed.

The one stale lead (`7ac3156e`, the message you sent be

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/app/api/bud/command/route.ts`, `tests/api/bud-command.test.ts`, `tests/api/messenger-ingest.test.ts`, `tests/lib/mission-control-layout.test.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [11:16] Session 75154746

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9A — Mission Control Queue Clarity Goal: Stop the Mission Control home page from mixin

**Dev OS agents:** bud-researcher, bud-qa

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`

**Summary:**
TypeScript and ESLint both pass clean. Here's what Phase 9A delivers:

**What changed** — `OverviewCore.tsx`, the `ActionQueue` component:

- "Needs a decision" (flat mixed list) is gone. Replaced by four distinct labelled sections inside the same card:
  - **Needs approval** (yellow badge) — only items whose `approval.action_type` is one of: `send_email`, `send_messenger`, `approve_repair_plan`, `approve_ux_evolution_plan`, `approve_deployment_plan`, `run_improvement_pipeline`, `flag_for_review

---
## [11:20] Session 75154746 — Phase 9B: Queue Deduplication

**Task:** Deduplicate Mission Control action queue cards so repeated Bud Observer and Quote Triage items collapse into one visible card.

**Files changed:**
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`

**Summary:**
Render-layer deduplication added to `ActionQueue` — no backend changes, all underlying records preserved.

### What was added

Four pure helpers inserted in `OverviewCore.tsx`:

| Helper | Purpose |
|---|---|
| `normalizeTitle(title)` | Lowercase, strip punctuation, collapse whitespace, cap at 80 chars — used as a stable comparison key |
| `deriveDeduplicateKey(item)` | Computes a semantic group key per item. `send_email` → `send_email:{quote_id}:{recipient}:{subject}`. `flag_for_review` → `flag_for_review:{source_agent}:{affected_area}:{title}`. Other approvals → `{action_type}:{target_table}:{target_id}`. Non-approval items → `{source}:{agent_id}:{title}` |
| `DedupedQueueItem` type | `{ item: BudOsQueueItem; duplicateCount: number; duplicateIds: string[] }` |
| `deduplicateQueueBucket(items)` | Groups items by key, picks the most-recent as representative, returns `DedupedQueueItem[]` |

### UI change

`QueueItemRow` gained a `duplicateCount: number` prop. When `duplicateCount > 1`, a `+N similar` chip renders in the metadata row alongside the agent name and status — no layout change, no action change.

### Architecture

- Deduplication runs in all four bucket arrays before the `slice(0, 10)` cap.
- The section count badge reflects the deduped item count (visible cards).
- Approve/Reject/Investigate buttons operate on the representative item's ID — the same behaviour as before. All duplicate IDs are stored in `duplicateIds` for future use (bulk-dismiss, audit trail).
- The existing backend dedup in `buildBudOsActionQueue()` (`target_table:target_id` key) is unchanged and complementary.

### TypeScript + ESLint: both pass (exit 0)

---
## Deploy — Phase 9A/B

**Commit:** `436d363` — `feat(mission-control): Phase 9A/B — queue clarity and deduplication`  
**Deployment:** `dpl_GHk8jenkeyr9Vgnwx86mRPiWXPk3` → `BUILDING` → production  
**Inspect:** https://vercel.com/budsatwork0-ais-projects/budsatworkaus/GHk8jenkeyr9Vgnwx86mRPiWXPk3  
**Previous production:** `ad4fb61` (Phase 8A/B/C)

**What lands:**
- Mission Control "Needs a decision" replaced with four labelled sections (Phase 9A)
- Render-layer dedup with "+N similar" chip (Phase 9B)
- No backend changes — pure render layer, TypeScript clean

---
## [11:24] Session 75154746

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9A — Mission Control Queue Clarity Goal: Stop the Mission Control home page from mixin

**Dev OS agents:** bud-researcher, bud-qa

**Files changed:**
- `Buds At Work/Dev/Dev Log 2026-06-03.md` *(re-edited)*
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*

**Summary:**
All done. Here's the summary:

**Committed** `436d363` — `feat(mission-control): Phase 9A/B — queue clarity and deduplication`
- [OverviewCore.tsx](src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx) — all Phase 9A/B changes
- `Buds At Work/Dev/Dev Log 2026-06-03.md` — Phase 9B entry + deploy note added

**Pushed** → `origin/main`

**Deploying** — `dpl_GHk8jenkeyr9Vgnwx86mRPiWXPk3` is `BUILDING` on production  
Inspect: https://vercel.com/budsatwork0-ais-projects/budsatworkaus/

> **Re-edit detected** on `Buds At Work/Dev/Dev Log 2026-06-03.md`, `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [11:25] Session 75154746

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9A — Mission Control Queue Clarity Goal: Stop the Mission Control home page from mixin

**Dev OS agents:** bud-researcher, bud-qa

**Files changed:**
- `Buds At Work/Dev/Dev Log 2026-06-03.md` *(re-edited)*
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*

**Summary:**
Graphify rebuild finished (exit 0). Everything is done:

| Step | Status |
|---|---|
| Commit `436d363` | `feat(mission-control): Phase 9A/B — queue clarity and deduplication` |
| Push → `origin/main` | Done |
| Vercel deploy `dpl_GHk8jenkeyr9Vgnwx86mRPiWXPk3` | `BUILDING` → production |
| Obsidian dev log | Phase 9B entry + deploy record written |
| Graphify graph | Rebuilt — `OverviewCore.tsx` changes indexed |

> **Re-edit detected** on `Buds At Work/Dev/Dev Log 2026-06-03.md`, `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [11:33] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Files changed:**
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`

**Summary:**
Here's what was done:

---

### Root cause confirmed

`flagForReviewEffect` had a silent-swallow regex `/relation .* does not exist/` but Supabase emits `"Could not find the table 'public.agent_alerts' in the schema cache"` — different message, so it threw instead of swallowing.

---

### Changes

**[supabase/migrations/086_agent_alerts.sql](supabase/migrations/086_agent_alerts.sql)**
- Creates `public.agent_alerts` with all required columns: `id`, `action_id`, `agent_id`, `source_agent`, `sever

> **Re-edit detected** on `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [11:36] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Files changed:**
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`

**Summary:**
The table is fully in place in production with exactly the right schema. The migration error was a false alarm — the `CREATE POLICY` line failed because the table (and its RLS policy) had already been applied to the database at some point. All columns, types, and nullability match the migration exactly.

**Phase 9C is complete and deployed:**

- `public.agent_alerts` — live in production with all 11 columns, unique index on `action_id`, RLS + service-role policy
- `runtime.ts` — `flagForReviewEf

> **Re-edit detected** on `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [11:59] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`
- `supabase/migrations/087_action_dedup.sql`

**Summary:**
Index is live and exactly as specified. Phase 9D is complete and deployed.

---

## Summary

### What changed

**[supabase/migrations/087_action_dedup.sql](supabase/migrations/087_action_dedup.sql)**
- Adds `action_identity text` column to `agent_actions`
- Backfills existing pending `send_messenger` rows: `send_messenger:<lead_id>:<conversation_id>`
- Backfills existing pending `send_email` rows: `send_email:<target_table>:<target_id>`
- Archives any pre-existing duplicate pending rows (status 

> **Re-edit detected** on `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:00] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`
- `supabase/migrations/087_action_dedup.sql`

**Summary:**
Everything is pushed and deployed:

- **Phase 9C** (`21285ed`) — `agent_alerts` table + `flagForReviewEffect` fix — live
- **Phase 9D** (`45c1449`) — `action_identity` dedup + partial unique index — live
- **Migration applied** to Supabase production (`action_dedup`) — confirmed
- **Vercel deploy** triggered on both pushes

> **Re-edit detected** on `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:09] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`
- `supabase/migrations/087_action_dedup.sql`

**Summary:**
Both queries returned empty — no duplicate pending identities exist, and the migration found no rows to supersede (the queue was already clean per-identity before Phase 9D ran). 

---

## Verification: Phase 9D — send_messenger / send_email dedup

**Verdict: PASS**

**Claim:** Duplicate pending communication approval cards should be collapsed to one per lead/quote. No messages should be sent.

**Method:** Production Supabase direct queries (UI inaccessible without auth via headless browser — dev

> **Re-edit detected** on `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:14] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/lib/agents/agents/customer-reply.ts` *(re-edited)*
- `src/lib/agents/agents/quote-triage.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`
- `supabase/migrations/087_action_dedup.sql`

**Summary:**
Phase 9E is live. Here's what changed and what the cards will look like on the next agent run:

---

### New label formats

**`send_messenger`** (customer-reply):
```
Messenger reply to Jackson Taylor · lead 5dd029 · 'Can you clean my bins?'
```

**`send_email` from lead** (customer-reply):
```
Email reply to Jackson Taylor · lead 5dd029 · 'Do you service Beenleigh?'
```

**`send_email` from quote** (quote-triage):
```
Quote email · window cleaning · $180 · 14 Drover Crescent · quote 7dd146
```


> **Re-edit detected** on `src/lib/agents/agents/customer-reply.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:17] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/lib/agents/agents/customer-reply.ts` *(re-edited)*
- `src/lib/agents/agents/quote-triage.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`
- `supabase/migrations/087_action_dedup.sql`

**Summary:**
All 7 rows updated. Phase 9F complete.

---

## Dry-run → Applied: before/after

| Type | Old | New |
|---|---|---|
| send_email | `Quote windows for Jackson Taylor at 177 Drover Cres, Flagstone QLD 4280, Australia — AUD $120` | `Quote email · windows · $120 · 177 Drover Cres · quote ab4fe4` |
| send_email | `Quote windows for Jackson Taylor at 177 Drover Cres, Flagstone QLD 4280, Australia — AUD $120` | `Quote email · windows · $120 · 177 Drover Cres · quote 7dd146` |
| send_messenger | `Messen

> **Re-edit detected** on `src/lib/agents/agents/customer-reply.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [12:28] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx` *(re-edited)*
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/agents/actions/bulk/route.ts`
- `src/lib/agents/agents/customer-reply.ts` *(re-edited)*
- `src/lib/agents/agents/quote-triage.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`
- `supabase/migrations/087_action_dedup.sql`

**Summary:**
Phase 10A is live. Here's what was built:

---

### How it works

**Eligible group detection** (`computeBulkGroups`): groups the `approvals` bucket by `action_type:source_agent:risk_level`. A group only surfaces the button if it has ≥2 items that are all `readiness='ready'`, `risk_level` is `low`/`medium`, and the action type isn't dangerous.

**UX flow**: button renders in the `QueueSubSection` header per eligible group — e.g. `Approve 5 Messenger drafts`. First click enters confirmation state,

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`, `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/lib/agents/agents/customer-reply.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
## [14:28] Session 081f553b

**Task:** <ide_opened_file>The user opened the file /Users/jacksontaylor/budsatwork/supabase/migrations/085_backfill_messenger_psid.sql in the IDE. This may or may not be related to the current task.</ide_opened_file> Phase 9C — Fix flag_for_review execution failure Problem: Approving a Bud Observer flag_for_

**Dev OS agents:** bud-researcher

**Files changed:**
- `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx` *(re-edited)*
- `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx` *(re-edited)*
- `src/app/api/agents/actions/bulk/route.ts`
- `src/lib/agents/agents/customer-reply.ts` *(re-edited)*
- `src/lib/agents/agents/quote-triage.ts`
- `src/lib/agents/runtime.ts` *(re-edited)*
- `supabase/migrations/086_agent_alerts.sql`
- `supabase/migrations/087_action_dedup.sql`

**Summary:**
Both stashes restored cleanly. Phase 10B is live — all your in-progress homepage work is back in the working tree, uncommitted and safe.

**Summary:** Phase 10B committed (`5b0aaaf`) and deployed via Vercel push. Agent Value now shows:
- **Needs attention** — any failing agent
- **Active** — high-value agents, sorted by pending approvals then run count (Customer Reply and Quote Triage surface first)
- **Support** — medium-value active agents
- **Footer** — `N low-value · N disabled ▼` with one-c

> **Re-edit detected** on `src/app/(app)/dashboard/mission-control/MissionControlClient.tsx`, `src/app/(app)/dashboard/mission-control/_components/OverviewCore.tsx`, `src/lib/agents/agents/customer-reply.ts`, `src/lib/agents/runtime.ts` — Claude corrected itself mid-session. If this was a convention violation, capture the rule:
> ```
> npx tsx scripts/vault-convention.ts
> ```

---
