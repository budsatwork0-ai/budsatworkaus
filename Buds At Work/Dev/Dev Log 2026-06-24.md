# Dev Log 2026-06-24

## [11:14] Session 436effda

**Task:** The green decorative icon/blob is still rendering behind the active service card. Do a deeper fix. Task: Find and remove the source of the hidden green icon/blob behind the “Full Window Clean” active card. Important: This is not a z-index issue anymore. Do not try to bring it forward. Remove the ele

**Files changed:**
- `src/app/(public)/services/page.tsx`

**Summary:**
Clean. Here's what I found and fixed:

**Source of the green icon:** Lines 3316–3324 in [services/page.tsx](src/app/(public)/services/page.tsx#L3316) — a "Tip" paragraph rendered when `!hasInteractedStep2` (i.e. on every initial page load). It contained:

- A `text-emerald-600` info circle SVG (`<circle cx="12" cy="12" r="10">`) — the green circle you were seeing
- A `-mb-3` **negative margin** that pulled the paragraph to within ~12px of the top of the scope card grid, making the icon appear to

---
