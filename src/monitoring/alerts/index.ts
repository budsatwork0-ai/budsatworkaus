/**
 * Alert registry — all rules registered here are discovered automatically
 * by the existing evaluation loop.
 */

export { agentSilentSuccessAlert } from './agent-silent-success';
export type { AlertRule } from './agent-silent-success';
